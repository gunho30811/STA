# 2026-07-30

## 카톡 채팅 알림을 알림 전용 카카오 앱으로 분리 (#283)

- '이용 중 동의'로 talk_message를 켰는데도 `invalid_scope` — 켠 곳이 사용자 본인 소유 앱
  (Rendit, ID 1518010, 비즈 승인됨)이고 사이트는 팀원 관리 로그인 앱(심사 진행 중)을 쓰고
  있었음. 로그인 앱을 건드리는 대신 **알림만 본인 앱으로 분리**하는 구조로 해결.
- `KAKAO_MSG_CLIENT_ID/SECRET` 환경변수 신설(로컬·서버 .env 반영). `/auth/kakao?notify=1`이
  메시지 앱 + `scope=talk_message`로 인가받고, 전용 콜백 `/auth/kakao/msg-callback`에서
  refreshToken 암호화 저장 + 즉시 테스트 발송. `kakao_notify.py` 토큰 갱신도 메시지 앱 우선
  (미설정 시 로그인 앱 폴백). 카카오 user id는 앱별로 달라서 매칭 대신 **로그인 세션 회원에
  토큰을 귀속**시키는 방식.
- 새 카카오 콘솔 삽질 기록: KOE205=동의항목 미설정, KOE006=리다이렉트 URI 미등록.
  리다이렉트 URI 등록 위치가 개편되어 **[앱] > [플랫폼 키] > [REST API 키] > 해당 키 수정**
  안에 있음(카카오 로그인 메뉴 아님 — 고급의 '로그아웃 리다이렉트 URI'·카카오싱크 플러그인과
  혼동 주의). 새로 발급하는 REST API 키는 클라이언트 시크릿이 기본 활성화라, 기존 키를
  수정하는 게 정석. → URI 등록 후 🔔 카톡알림 버튼 재시도·서버 로그 확인 예정(진행 중).

## db.py 주석 정리 + 관리자 계정 하드코딩 제거

- db.py 주석 전반 정리: 파일 헤더를 현 구조(메인=오라클 내부 pg, 네이버 크롤만 Supabase
  중간 적재소 경유)에 맞게 다시 씀. Railway·Vercel·GH Actions 등 옛 인프라 언급 제거,
  outbox 발송 주체를 서버 크론 폴러로 정정, "매주 크롤" → 회차 기준으로 수정.
- **`_seed_admin` 하드코딩 기본값(gunho/rjsgh1004!) 제거** — ADMIN_USERNAME/ADMIN_PASSWORD
  환경변수가 둘 다 있을 때만 시드하도록 변경. `.env.example`의 기본값 언급도 삭제.
  ⚠ 퍼블릭 레포 git 히스토리(#49)에 이미 노출된 값이므로, 코드 제거와 별개로
  **DB의 실제 관리자 비밀번호 교체 필요**. → 서버 DB에서 교체 완료(해시 검증까지 확인).

## 네이버 크롤 Supabase 경유 폐지 → 오라클 직행

- 오늘 02:00 네이버 크롤이 `tenant/user not found`로 즉사 — **Supabase 프로젝트가 정지/삭제**된
  상태. 데이터는 이미 오라클에 전부 있고(listings 853k, 07-29분까지) Supabase는 중간 경유지라
  유실은 없음.
- `crawl_naver_local.py`를 삼삼과 같은 **SSH 터널 직적재**로 재작성: 로컬 포트 15433(삼삼
  15432와 동시 실행 가능), 크롤 후 **적재 건수 검증**(crawl_state 집계 대비 80% 미만이면
  rc=2) — 예전 유실 사고(93k→15건) 재발 감지용.
- 공용 터널 코드 보강(`crawl_samsam_local.py`): SSH keepalive 15초 + 채널 열기 3회 재시도.
- 서버 크론 `/etc/cron.d/rendit-sync`에서 죽은 sync_from_supabase(6시간) 줄 제거, 사용자
  백업(04:40)은 유지. Supabase 관련 스크립트 파일들은 혹시 몰라 삭제하지 않고 남김.
- 새 경로 첫 실행 검증: 138,512/138,517건(99.996%) 적재, 뷰 갱신까지 rc=0, 약 2시간.

## 삼삼 유형 확장 + 렌트분석 비수도권 처리

- 삼삼 일일 크롤이 사실상 **오피스텔만 예약률 갱신** 중이었음(계정당 하루 예약조회 한도
  ~5,000건). 나머지 5종(원룸건물·연립빌라·단독·아파트·상가주택, 수도권 ~9.5k건)은 예약률이
  7/17~7/20에 동결 → 오늘 오피스텔 크롤 종료 후 나머지 유형을 계정당 1,500건 한도로 이어
  돌리도록 백그라운드 예약(차단 방지 위해 부분 갱신, 오래된 순).
- 렌트분석에 비수도권 5,020건(부산 1,339·제주 508·강원 500…, 6/26~7/1 수집분 동결)이
  수도권과 섞여 표시되던 문제 → **화면(samsam_app 로드)에서만 수도권 필터, DB는 보존**.
- **[기록] 분석 리포트 계획**: 매일 크롤로 쌓이는 데이터(samsam_snapshots 예약률 추이,
  samsam_churn 매물 증감, 비수도권 스냅샷 포함)로 나중에 분석 리포트를 만들 예정 —
  그래서 오래된 데이터도 DB에서 삭제하지 않고 유지한다(사용자 결정).

## 통합 크롤러 .exe (네이버 + 삼삼, 어디서든 → 운영 DB)

- `gui/rendit_all.py` — 기존 네이버 GUI·삼삼 GUI를 탭 하나짜리 프로그램으로 통합.
- **저장 위치 3모드(공통)**: ① rendit 서버(권장) — SSH 비밀번호만 넣으면 오라클에 터널을
  뚫고 pg 비밀번호는 서버 .env에서 자동 조회, 직적재(포트 15434 — 운영 크롤 15432/15433과
  안 겹침). 네이버 서버모드는 크롤 후 nl_live 뷰 갱신까지 해서 사이트 즉시 반영.
  ② DB URL ③ 로컬 SQLite(naver.db/samsam.db + CSV).
- **자동 다운로드**: 크롬 엔진(최초 1회, %LOCALAPPDATA%\ms-playwright), 예약률 30%+ 타겟
  동 목록(GitHub 최신본, 실패 시 번들본). 삼삼 탭엔 계정당 하루 예약조회 한도 스핀박스
  (기본 4,000 — 초과 시 차단 경고).
- PyInstaller onefile+windowed 62MB. Python 3.15 sqlite3 수동 번들 + `--collect-all
  playwright`. frozen `--selftest`/`--browsertest` PASS, 서버 터널 모드 실쿼리 검증
  (listings 867k/samsam 21k). 배포: 바탕화면 `rendit통합크롤러.exe`(레포 커밋 안 함).
- **v2 (사용자 요청)**: CSV 내보내기를 저장 위치와 **별개의 공통 옵션**으로 — 폴더 지정 시
  어느 저장 모드든(서버/DB URL/로컬) **이번 실행분**을 `naver_매물_시각.csv` /
  `samsam_매물_시각.csv`로 병행 저장(서버 터널로 4,196건 실검증). 버그 2건 수정:
  터널 close 시 `server_close()` 누락(연속 실행 시 포트 점유), 포트 충돌 시 15434~15439
  자동 탐색(프로그램 2개 동시 실행 대응 — 실제로 사용자가 exe 실행 중에 발견).
  구버전 exe가 실행 중이라 새 빌드는 `rendit통합크롤러_새버전.exe`로 배포.

## 🚨 통합채팅 답장 오발송 버그 수정 (fix/chat-send-room-disambiguation)

- **증상**: 김수정 방에 답장했는데 남승영에게 발송됨. 실DB로 재현 확인 —
  계정#17에 room_name `오픈특가♥︎강남역💕역삼역` 방이 **7개**, 김수정(room 498517/
  key 968262/회원 688187)과 남승영(room 495585/key 953282/회원 822809)이 이름만 같음.
  outbox#4는 김수정 방(498517)에 큐잉됐는데 발송됨.
- **원인**: `chat_auth.send_message`가 방을 **room_name 텍스트로만** 찾아
  `pg.locator('li', has_text=room_name).first` 클릭 — 같은 이름 방 다수면 `.first`
  (목록 최상단=최근 대화=남승영)가 선택됨. 방 고유키·상대 회원ID를 DB가 다 알면서도
  발송만 이름으로 매칭한 게 근본 원인.
- **수정(여러 겹, 전부 fail-closed — 애매하면 안 보냄)**:
  1. `chat_poll._verify_room_live` 신설: 발송 직전 RTDB `live/chatlist`로 ①room_key 생존
     ②상대 회원ID 일치 ③host 방 교차확인. **삭제(목록에서 사라짐)된 방은 'blocked'로 보류**
     — 사용자 정책 "삭제된 방엔 상대가 다시 연락할 때까지 발송 금지". (cancel 상태는 방이
     살아있으면 발송 허용 — 사용자 결정.)
  2. `send_message`에 `counterpart_nickname`·`samsam_room_key` 인자 추가. 목록에서
     **매물명 + 상대 닉네임으로 정확히 1개**일 때만 클릭(0개·2개↑면 SendError로 중단).
     클릭 후 **열린 방에 그 닉네임이 보이는지 재확인** 뒤에야 입력·전송.
  3. `process_outbox` 쿼리에 room_key·counterpart_member·nickname·refresh_token_enc 추가.
- **검증**: py_compile OK. `_verify_room_live` 단위테스트 6종(정상 통과/삭제 보류/신원
  불일치 차단/게스트 차단/토큰없음/남승영-김수정 교차 차단) 전부 PASS. UI 특정 로직은
  실서버 필요라 미검증이나 fail-closed라 최악도 '미발송+로그'(오발송 아님).
- **남은 것**: ① 서버 배포(git pull + docker compose up web) ② 실제 답장 1건으로
  엔드투엔드 확인 ③ (후속) 프론트가 outbox 'blocked'/'failed' 상태를 사용자에게 표시 —
  지금은 큐잉 즉시 "전송"처럼 보이나 실제 발송은 폴러 몫이라 미발송을 사용자가 모를 수 있음.

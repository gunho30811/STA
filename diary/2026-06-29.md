# 2026-06-29

- **회원관리 + 로그인 게이트 신설** (`web/auth.py`, `db.py` users 테이블):
  - 요구사항: 관리자 gunho/rjsgh1004!, 회원가입은 이름·생년월일·이메일 + 비밀번호(특수문자 필수),
    이메일 인증(전화인증은 취소), 5001~5003 전부 로그인해야 접근.
  - `db.py`: `users` 테이블(username/email/password_hash/name/birthdate/role/email_verified/
    verify_code/verify_expires) + `_seed_admin`(init_db 첫 실행 때 gunho 1회 시드, 비번 해시).
  - `web/auth.py`: Flask Blueprint(`/auth/login|logout|signup|verify|members`) + `init_auth(app)`.
    - 게이트: before_request로 모든 라우트 보호, `/auth/*`·static만 허용, 미로그인 시 로그인으로 리다이렉트.
    - 비번 정책: 8자+ & 특수문자 필수(werkzeug 해시 저장).
    - 이메일 인증: **목(mock) 모드** — 6자리 코드를 서버로그+화면(dev)에 표시. `send_verify_email`만
      SMTP로 바꾸면 실제 발송. 인증 전 회원은 로그인 차단(관리자는 예외).
    - 회원관리(`/auth/members`): 관리자만, 목록(이메일·이름·생년월일·인증여부·가입일) + 삭제.
  - 4개 뷰어(app/gangnam/profit/samsam)에 `init_auth(app)` 연결. localhost 쿠키는 포트 무시라 같은
    SECRET_KEY면 5001~5003 로그인 공유.
  - `.env`에 SECRET_KEY 추가(+ .env.example 안내). ⚠️ append 시 개행 없어 DATABASE_URL 끝에 붙는
    버그로 DB 인증 깨졌다가 복구(분리). 
  - 검증: 게이트 차단(302)→관리자 로그인(200)→접근, 가입→이메일인증(목)→로그인, 미인증 거부,
    특수문자 없는 비번 거부, 회원관리 목록/삭제 전부 통과. 테스트 계정 정리 완료.
- **SMTP 실발송 연동**: `send_verify_email`을 smtplib 기반 실발송으로 교체. SMTP_HOST/USER/PASS
  설정 시 실제 메일 발송(465=SSL, 그외 STARTTLS) 후 None 반환→화면에 코드 미노출. 미설정/실패 시
  목 폴백(코드 화면 표시). Gmail 앱비밀번호 사용. `.env.example`에 SMTP_* 안내 추가. 미설정 폴백 검증.
- **Gmail SMTP 실발송 확인 + 운영 보강**:
  - `.env`에 SMTP 입력 → 실제 발송 성공 검증(gunho30811@gmail.com 수신, 반환 None=화면 코드 미노출).
    앱비번 공백 붙여넣기 대응(`.replace(" ","")`).
  - **테이블명 충돌 해소**: Supabase 예약 `auth.users`와 헷갈리던 `public.users` → `public.members`로
    리네임(DB ALTER + db.py/auth.py 전체 쿼리 치환). 리네임이 누수된 idle-in-transaction 락에 막혀
    pg_terminate_backend로 정리 + lock_timeout 후 성공.
  - **하루 가입 한도** 추가: `DAILY_SIGNUP_LIMIT`(기본 10) — 오늘 가입 회원수 ≥ 한도면 차단(스팸 방지).
- **Vercel 호스팅 준비: 통합 포털(`web/portal.py`)**:
  - 3개 뷰어(profit/samsam/gangnam)를 한 주소에 마운트 + 단일 로그인. DispatcherMiddleware로
    `/profit` `/samsam` `/gangnam` 마운트, `/`는 랜딩(카드 링크+관리자 회원관리), `/auth/*` 인증.
  - 템플릿 fetch 11곳을 절대→상대경로(`fetch('api/...')`)로 변경 — 마운트 prefix 하에서도, 독립 실행
    (5001~5003)에서도 동작. 쿠키 path=/ 라 로그인 1회로 전 마운트 공유.
  - `api/index.py`(portal application 노출) + `vercel.json`(rewrites + installCommand: flask/
    psycopg2-binary/python-dotenv — playwright 제외). db.py는 Vercel에선 psycopg2 사용.
  - 로컬 통합 검증(포트 8000): 게이트 차단(302)→관리자 로그인→랜딩→마운트 뷰어 3개 200 +
    상대 API(/profit/api 등) 200 + 미로그인 시 마운트도 차단. 실제 배포는 Vercel 계정/CLI로 진행 예정.
  - **Vercel 배포 완료**: GitHub 연동 → https://sta-gray.vercel.app . 환경변수(DATABASE_URL/SECRET_KEY/
    SMTP_*/ADMIN_PASSWORD) 입력. main 머지 시 자동 재배포. 미로그인 시 /samsam/ 등 마운트·API 전부 302
    차단 확인(데이터 미노출, 안전). 보이던 건 기존 로그인 쿠키 때문.
- **인증번호 재발송** (`/auth/resend`): verify 화면에 "다시 보내기" 버튼. 60초 쿨다운(verify_expires에서
  발송시각 역산)으로 남용 방지, 통과 시 새 코드 발급+재발송. 검증: 즉시 재발송 차단→쿨다운 후 새 코드.
- **관리자 최종 승인제**: 회원은 이메일 인증 + **관리자 승인**까지 돼야 로그인 가능.
  - `members.approved` 컬럼 추가(관리자 자동 TRUE). 로그인 시 미승인 회원은 "관리자 승인 대기" 차단.
  - 회원관리 페이지에 승인/대기 상태 + 승인·승인취소·삭제 버튼, 대기 인원수 표시, 미승인 상단 정렬.
  - 검증: 미승인 로그인 차단→관리자 승인→로그인 성공 end-to-end.
- **공통 네비게이션 바**: `init_auth` after_request로 로그인 상태 HTML 페이지 상단에 자동 주입(템플릿
  수정 없이 전 뷰어 공통). 홈/수익성/삼삼분석/강남매물/로그아웃 + (관리자)회원관리. 세션에 role 저장해
  관리자 메뉴 노출 판단. 절대경로라 마운트/독립실행 양쪽 동작.
- **Vercel 콜드스타트 hang + 500 긴급 수정**: 첫 요청 30~60초 멈춤/500. 원인 3개 잡음:
  1. portal이 import 시 3뷰어 데이터(강남 22MB+삼삼DB+CSV)를 전부 로딩 → **lazy 로딩**으로 전환
     (해당 뷰어 첫 진입 때만). portal import 30s+ → **0.36s**.
  2. `init_db`가 콜드스타트마다 4회(앱마다) 실행 + ALTER가 ACCESS EXCLUSIVE 락 노려 hang →
     프로세스당 1회 가드 + **스키마 있으면 DDL 전체 스킵**(빠른 SELECT 1회, 0.21s). ALTER는 신규설치만.
  3. SELECT 후 "idle in transaction"으로 연결이 묶여 Supabase 풀러 고갈 → **autocommit=True**.
     auth `current_user` 연결 누수도 close. `vercel.json` maxDuration 60s.
# 2026-07-02

## 삼삼엠투 통합 채팅 (여러 계정 연결)

한 회원이 삼삼엠투 계정을 여러 개 쓸 때, 임대인 모드 채팅(손님 문의)을 한 곳에서 보게 하는 기능.

- **정찰**: 삼삼엠투 채팅은 REST가 아니라 Firebase RTDB(`spacev-33m2-rdb`)를 웹소켓으로 직접 씀.
  로그인 시 33m2 백엔드가 Firebase 커스텀 토큰 발급 → `signInWithCustomToken`으로 교환한 `refreshToken`만
  있으면, 이후엔 브라우저 없이 `securetoken.googleapis.com`(토큰 갱신) + RTDB REST(`live/chatlist/{uid}`,
  `live/messagelist/{room}`)만으로 폴링 가능함을 확인.
- **스키마**(`db.py`): `samsam_accounts`(회원↔삼삼계정, 비번·refreshToken은 `crypto_util.py`(Fernet)로
  암호화 저장) / `samsam_chat_rooms` / `samsam_chat_messages`. 계정당 여러 개 연결 가능하게 정규화 테이블로.
- **`pipeline/samsam/chat_auth.py`**: Playwright 로그인 1회 → refreshToken 획득. 이후 갱신은 순수 HTTP.
- **`pipeline/samsam/chat_poll.py`**: 계정별 폴링. refreshToken 만료 시 저장된 비번으로 자동 재로그인.
  `host_or_guest` 필드로 임대인 모드 채팅만 저장(게스트로 예약한 채팅은 제외).
- **웹**(`web/samsam_app.py` + `templates/samsam_chat.html`): `/samsam/chat/`에서 계정 연결 폼 +
  통합 인박스(방 목록 + 메시지 스레드). "지금 새로고침" 버튼은 그 회원 계정만 즉시 폴링.
- 로컬에서 실계정으로 연결→폴링→UI까지 end-to-end 확인 후, 테스트로 넣었던 계정은 삭제(다른 회원에게
  안 보이게).
- 다음 단계(미착수): 주기적 폴링용 GH Actions workflow.

## 통합채팅 주기적 폴링 workflow + CHAT_ENC_KEY 누락 발견 (feat/samsam-chat-poll-workflow)

- 위 "다음 단계"에 착수하며 `chat_poll.py` 필요 시크릿을 점검하다 **`CHAT_ENC_KEY`가 로컬 `.env`·
  GH secret·`.env.example` 어디에도 없음**을 발견. `SAMSAM_EMAIL/PASSWORD`(크롤러 봇 계정)와 헷갈리기
  쉬운데 별개 용도(계정 비번·refreshToken 암호화)임을 확인.
- DB(`samsam_accounts`) 조회 결과 연결된 계정 0건 — 기존 암호문에 종속된 값이 없어 신규 키 생성이
  안전함을 확인 후 생성 → GH secret 등록. Vercel 프로덕션 쪽은 같은 값을 사용자가 직접 등록.
- `.env.example`에 `CHAT_ENC_KEY` 설명·생성법 추가(재발 방지), 로컬 `.env`에도 반영.
- `pipeline/samsam/decrypt_tool.py` 신설: 암호문 하나를 CLI로 바로 복호화(운영/디버깅용, `chat_poll.py`
  본 폴링 로직과 분리).
- `.github/workflows/samsam-chat-poll.yml` 신설: 10분마다 `chat_poll.py` 실행(+ 수동 실행). 재로그인
  대비 Playwright Chromium도 설치. DB에만 쓰고 커밋할 파일이 없어 `crawl-samsam.yml`과 달리
  PR 자동 생성 단계 없음.
- **프로덕션 실사용 중 발견한 진짜 문제**: 사용자가 실제 계정으로 `/chat/`에서 연결 시도 →
  `ModuleNotFoundError("No module named 'playwright'")`. 원인은 계정 연결(`chat_api_add_account`)이
  로그인을 Vercel 요청 안에서 즉시 처리하는데, Vercel엔 Playwright가 아예 없음(`vercel.json`
  installCommand에서 의도적으로 제외 — 서버리스에 브라우저 자동화 불가). 로컬에서만 테스트해서
  못 잡았던 것으로, **연결 기능이 프로덕션에서 원천적으로 항상 실패**하는 상태였음.
  - 수정: `chat_api_add_account`에서 `ModuleNotFoundError`를 잡으면 즉시 실패 대신 비번만
    암호화해 `status='pending_login'`으로 큐잉하고 안내 메시지 반환.
  - `chat_poll.py`의 `poll_account`를 리팩터: `refresh_token_enc`가 없는(최초 로그인 대기) 계정도
    기존 "재로그인" 경로(비번 기반 로그인)로 통합 처리 — GH Actions(Playwright 설치됨)가 다음
    폴링 주기(10분 이내)에 실제 로그인을 대신 완료.
    프론트(`samsam_chat.html`)에 `pending_login` 상태 배지("연결 처리 중")·안내 메시지 추가.
  - 로컬에서 mock으로 4가지 분기(최초 로그인 성공/실패, 갱신 성공, 갱신 실패→재로그인) 검증.

## 통합채팅 1분 간격 폴링 (GH Actions 10분으론 너무 느리다는 피드백)

- GH Actions 예약 실행은 5분 미만은 사실상 의미 없고(공식 권장 최소 5분), 그마저도 부하 시간대엔
  수분씩 밀림 — "1분 간격"을 GH Actions cron으로는 못 만듦.
- 대신 폴링을 둘로 분리: **메시지 조회**(refreshToken 갱신 + RTDB 조회, 순수 HTTP)는 Vercel에서도
  바로 돌아가므로 외부 무료 크론 서비스(cron-job.org 등)가 1분마다 호출. **로그인/재로그인**(Playwright
  필요)은 기존 GH Actions 10분 스케줄이 계속 전담.
  - `pipeline/samsam/chat_poll.py`: 계정 순회 로직을 `poll_all(conn)`으로 뽑아 `main()`과 신규
    엔드포인트가 공용으로 사용.
  - `web/samsam_app.py`: `/chat/api/cron-poll`(신규) — `CRON_SECRET` 쿼리파라미터(`hmac.compare_digest`
    로 상수시간 비교) 검증 후 `poll_all` 실행. 세션 로그인 없이 외부에서 호출하는 용도.
  - `web/auth.py`: 전역 로그인 가드(`before_request`)가 모든 라우트를 세션 체크하는데, 이 엔드포인트는
    예외 처리 추가(자체 시크릿 검증으로 대체 보호) — 안 하면 항상 로그인 페이지로 리다이렉트되던 버그를
    로컬 테스트 중 발견해 즉시 수정.
  - Flask test_client + mock으로 무인증/틀린키/맞는키 3케이스 로컬 검증(403/403/200).
  - 다음 단계(사용자): Vercel에 `CRON_SECRET` 환경변수 등록 + cron-job.org 같은 서비스에
    `GET https://sta-gray.vercel.app/chat/api/cron-poll?key=<값>` 1분 간격 등록.
- **URL 실수**: `samsam_app`이 포털(`web/portal.py`)에서 `/samsam` prefix로 마운트되는 걸 놓쳐서
  처음 안내한 주소(`/chat/api/cron-poll`)가 전부 틀렸음(404/302). 실제 주소는
  `/samsam/chat/api/cron-poll` — curl로 직접 검증 후 정정.
- **진짜 회귀 발견**: 사용자가 실계정 연결 시도 → "재연결 필요"로 바로 바뀜. 원인은 1분짜리 Vercel
  크론이 GH Actions(10분, Playwright 있음)보다 훨씬 먼저 돌면서, `poll_account`가 로그인 필요
  여부만 보고 "이 환경에서 로그인 가능한지"는 구분 안 해 Vercel에서도 로그인을 시도 → 항상
  Playwright 없어 실패 → GH Actions가 오기도 전에 `reauth_needed`로 덮어씀.
  - 수정: `chat_auth.playwright_available()` 추가. `poll_account`는 로그인이 필요한데 이 환경에
    Playwright가 없으면 상태를 안 건드리고 그냥 스킵(다음 폴러에게 맡김) — GH Actions에서만 실제
    로그인을 시도·보고하도록 분리.
  - mock으로 6개 분기(Vercel/GH Actions × pending·만료·정상) 재검증.

## 계정 연결 즉시 로그인 트리거 (10분 대기 불만 피드백)

- 로그인이 GH Actions 10분 스케줄에 걸려 있어 최악 10~15분 걸리는 게 느리다는 피드백 → 계정
  연결 순간 GitHub API로 `samsam-chat-poll.yml`을 `workflow_dispatch`로 즉시 실행시키도록 추가.
  - `web/samsam_app.py`: `_trigger_chat_poll_workflow()` — `GH_DISPATCH_TOKEN`(선택, Fine-grained
    PAT) 있으면 GitHub API 호출, 실패/미설정 시 조용히 무시(기존 10분 스케줄이 안전망으로 남음).
    `chat_api_add_account`의 pending_login 큐잉 직후 호출.
  - `.env.example`에 `GH_DISPATCH_TOKEN` 발급 방법 문서화.
  - mock으로 토큰 없음/있음 두 경우 호출 여부 로컬 검증.
  - 다음 단계(사용자): github.com/settings/tokens에서 이 저장소 한정 Fine-grained PAT
    (Actions: Read and write) 발급 → Vercel 환경변수 `GH_DISPATCH_TOKEN`으로 등록.

## 통합채팅 답장 기능 + 미확인 방 구분 + AJAX 자동 갱신

- **정찰**(실계정 2개로 실제 발송 1회씩 수행, 사용자 승인 하에 "안녕하세요"/"네"만 전송): 삼삼 메시지
  "쓰기"는 로그인과 달리 Firebase REST/RTDB가 아니라 **Next.js 서버 액션**(`POST /host/chat`,
  배포마다 바뀔 수 있는 `Next-Action` 해시 헤더 필요)이라 그 내부 API를 직접 흉내내는 건 깨지기
  쉬움 → 로그인처럼 **실제 브라우저(Playwright)로 UI 조작**(방 클릭 → 입력 → 전송 버튼)해서 보내는
  방식으로 결정. 안전을 위해 정찰은 본인 소유 계정 2개(`97nkvd@gmail.com` 호스트 ↔
  `gunho30811@naver.com` 게스트) 사이 대화로만 진행.
- **스키마**: `samsam_chat_rooms.last_read_at`(미확인 방 판별용, epoch ms) + 신규
  `samsam_chat_outbox`(방별 답장 큐, status pending/sent/failed) — DDL은 `db.py`에 추가했지만
  `init_db()`가 `members` 테이블 존재 시 전체 DDL을 스킵하는 구조라, 이번 세션에서
  `db.init_db(force=True)`로 프로덕션 DB에 1회 직접 적용함(배포만으로는 자동 적용 안 됨).
- `pipeline/samsam/chat_auth.py`: `send_message(email, password, room_name, message)` 추가
  (로그인 폼 코드를 `_login_ui()`로 뽑아 공유). `pipeline/samsam/chat_poll.py`:
  `process_outbox(conn)` 추가 — `playwright_available()`일 때만(GH Actions) pending 답장을
  실제 발송, Vercel 1분 cron은 안 건드림(로그인 회귀 때와 같은 원칙).
  - mock으로 4케이스(Vercel 스킵/발송성공/발송실패/큐 없음) 검증.
- `web/samsam_app.py`: `/chat/api/rooms/<id>/send`(POST, outbox 큐잉 + 즉시 GH Actions 트리거
  재사용) 신설. `/chat/api/rooms`에 `unread` 플래그 추가, `/chat/api/rooms/<id>/messages`는
  열람 시 `last_read_at` 갱신(읽음 처리) + 그 방의 pending 답장(전송 대기중 표시용)도 같이 반환.
  - mock DB로 소유권 체크(404)·큐잉 INSERT 로직 검증(실제 발송 큐에 넣지 않도록 실DB에는
    안전한 케이스만 실행).
- `templates/samsam_chat.html`: 방 목록을 "미확인"/"전체" 두 그룹으로 분리 표시(빨간 점 배지),
  방 열람 시 답장 입력창 노출, 전송 중 메시지는 흐리게 "전송 대기 중" 표시. 새로고침 버튼 없이도
  20초 간격 setInterval로 방목록/현재 방을 다시 불러오게 함(실제 삼삼 폴링은 서버쪽 cron이 이미
  하고 있어 여긴 그 결과를 화면에 반영만 함 — 추가 크롤 트리거 아님).
- 답장은 최초 로그인과 마찬가지로 GH Actions 10분 스케줄(또는 즉시 트리거 시 ~1분) 안에 실제
  발송됨 — 큐잉 직후 화면엔 바로 안 보이고 "전송 대기 중"으로 표시되다가, 다음 폴링에서 발송+
  수신 확인까지 되면 정식 말풍선으로 바뀜.

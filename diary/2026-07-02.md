# 2026-07-02

## 삼삼엠투 통합 채팅 (여러 계정 연결)

한 회원이 삼삼엠투 계정을 여러 개 쓸 때, 임대인 모드 채팅(손님 문의)을 한 곳에서 보게 하는 기능.

- **정찰**: 삼삼엠투 채팅은 REST가 아니라 Firebase RTDB(`spacev-33m2-rdb`)를 웹소켓으로 직접 씀.
  로그인 시 33m2 백엔드가 Firebase 커스텀 토큰 발급 → `signInWithCustomToken`으로 교환한 `refreshToken`만
  있으면, 이후엔 브라우저 없이 `securetoken.googleapis.com`(토큰 갱신) + RTDB REST(`live/chatlist/{uid}`,
  `live/messagelist/{room}`)만으로 폴링 가능함을 확인.
- **스키마**(`db.py`): `samsam_accounts`(회원↔삼삼계정, 비번·refreshToken은 `crypto_util.py`(Fernet)로
  암호화 저장) / `samsam_chat_rooms` / `samsam_chat_messages`. 계정당 여러 개 연결 가능하게 정규화 테이블로.
- **`pipeline/samsam/chat_auth.py`**: Playwright 로그인 1회 → refreshToken 획득. 이후 갱신은 순수 HTTP.
- **`pipeline/samsam/chat_poll.py`**: 계정별 폴링. refreshToken 만료 시 저장된 비번으로 자동 재로그인.
  `host_or_guest` 필드로 임대인 모드 채팅만 저장(게스트로 예약한 채팅은 제외).
- **웹**(`web/samsam_app.py` + `templates/samsam_chat.html`): `/samsam/chat/`에서 계정 연결 폼 +
  통합 인박스(방 목록 + 메시지 스레드). "지금 새로고침" 버튼은 그 회원 계정만 즉시 폴링.
- 로컬에서 실계정으로 연결→폴링→UI까지 end-to-end 확인 후, 테스트로 넣었던 계정은 삭제(다른 회원에게
  안 보이게).
- 다음 단계(미착수): 주기적 폴링용 GH Actions workflow.
- PR #69로 머지됨.

## 동시 작업 브랜치 충돌 (교훈)

같은 로컬 폴더(`C:\Users\gunho\STA`)에서 나(에이전트)와 다른 세션/작업(크롤링 액션 수정, 예약률
로테이션 갱신 등)이 동시에 git 브랜치를 넘나들며 작업 중이었음 — 내 커밋이 엉뚱한 브랜치
(`chore/diary-2026-07-01-actions-fix`)에 들어가는 사고 발생. `git cherry-pick`으로 올바른 브랜치에
옮기고 `git reset --hard`로 잘못된 브랜치는 원복해서 복구.
- **교훈**: 이후 작업(접속자수, Vercel 핫픽스)은 `git worktree add ../STA-xxx -b feature/...`로
  완전히 분리된 디렉터리에서 진행해 충돌 원천 차단. 앞으로도 같은 폴더에서 여러 세션이 동시에
  브랜치 전환할 가능성이 있으면 worktree 격리를 우선 고려할 것.

## Vercel 프로덕션 크래시 → 긴급 핫픽스 (PR #71)

채팅 기능(#69) 머지 직후 실제 배포본이 500(FUNCTION_INVOCATION_FAILED)으로 전부 죽음.
- **원인 1**: `web/samsam_app.py`가 모듈 최상단에서 `chat_auth`를 import하는데, `chat_auth.py`가
  `playwright`를 최상단에서 import — Vercel 서버리스엔 브라우저 자동화가 없어 앱 전체 기동 실패.
- **원인 2**: `vercel.json`의 `installCommand`가 `flask psycopg2-binary python-dotenv`만 설치 —
  새로 추가한 `crypto_util.py`(`cryptography`)·`chat_auth.py`(`requests`) 의존성이 빠져 있었음.
- **수정**: `chat_auth.py`의 playwright import를 로그인 함수 안으로 지연(lazy) + `installCommand`에
  `requests cryptography` 추가.
- **남은 제약**: "삼삼 계정 연결"(신규 로그인, Playwright 필요)은 Vercel 서버리스 구조상 원천적으로
  불가능 — 로컬(또는 향후 별도 상시 서버)에서만 가능. 폴링·조회는 REST 기반이라 Vercel에서도 정상.
- **다음 결정 필요**: 이 저장소는 **public repo**라서 GitHub Actions workflow_dispatch에 비밀번호를
  입력값으로 넘기는 방식은 절대 불가(로그에 평문 노출). 다른 회원도 웹에서 계정 연결을 하려면
  Playwright를 돌릴 수 있는 별도 상시 서버(Railway/Render/Fly.io 등)를 두고 그쪽에 로그인 단계만
  위임하는 구조가 필요 — 아직 미착수, 다음 세션에서 진행 여부 결정.

## 현재 접속자수 표시

로그인 여부와 무관하게 최근 5분 내 활동한 세션 수를 네비바에 표시(PR 대기, 브랜치
`feature/online-visitor-count`).
- `db.py`: `visitor_pings(session_id PK, last_seen)`.
- `web/auth.py`: `before_request`에서 매 요청마다 핑 기록(`_ping_visitor`), `/auth/api/online`
  집계 API, 네비바에 배지 + 20초 폴링 JS.
- 테스트 중 실제 값이 부풀어 보인 적 있었음 — 원인은 (1) 테스트 스크립트가 새 세션을 여러 개
  만들어 같은 DB에 섞여 들어감, (2) 쿠키 없는 첫 요청이 여러 탭에서 동시에 뜨면 순간적으로
  중복 세션이 생김(5분 창이라 자연 정리됨). 로직 자체(같은 세션 재핑 시 중복 안 됨)는 검증 완료.
- 아직 이 브랜치는 push만 해둔 상태, PR 생성/머지 전.

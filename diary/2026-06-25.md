# 2026-06-25

- **삼삼엠투 크롤러 + 통합 파이프라인 DB 전환** (`feature/samsam-pipeline`):
  - `pipeline/samsam/crawler.py`: 삼삼 전체 매물 수집 → `samsam_listings` 적재
    - 로그인(Playwright) + 요청(requests), classify_btype.py 방식 그대로 재사용
    - 상세(`/v1/use-auth/rooms/{rid}`) + 스케줄(`/v1/use-auth/rooms/{rid}/schedules`) API 호출
    - 주소에서 층/건물명/시도/동 파싱, `subway.py`로 역 거리 계산
    - 이어받기(기본), `--redo`, `--limit N` 옵션 지원
  - `db.py`: `samsam_listings` 테이블 DDL + PK(`room_id`) + 인덱스 3개 추가
  - `pipeline/integrate/build_integrated.py`: JSONL 파일 의존 완전 제거
    - `samsam_listings` + `naver_listings` 두 DB 테이블에서 읽도록 전환
    - 매칭: `building_name`(직접 필드) + `floor`(int) + `area_m2` + 좌표
    - `_parse_nav_floor`/`_floor_category`/`_parse_sam_floor`/`bldg()`/`load_jsonl()` 제거
- 06-24 작업(Vercel 배포, `feature/vercel-deploy`)과 PR #21(GH Actions 주간 크롤 자동화,
  `naver_listings` 기반 전환) 확인. `crawl.yml`은 매주 월요일 10시(KST) 스케줄로 active 상태지만
  아직 한 번도 실행된 적 없음(다음 월요일 06-29 첫 실행).
- **네이버 매물종류 확장**: 기존 오피스텔 월세 단일종 → 아파트/오피스텔/빌라/원룸/단독다가구/상가
  6종으로 확대. 라이브 API 직접 호출로 realEstateType 코드(APT/OPST/VL/OR/DDDGG/SG) 확정.
  - `pipeline/naver/crawler.py`: `--types` 옵션 추가(기본값 OPST, 기존 주간 크롤 영향 없음).
    `crawl_state` 키를 OPST는 기존 그대로, 신규 타입은 `cortarNo:타입코드` 복합키로 분리해서
    스키마 변경 없이 타입별 진행상태 추적.
  - `detail_map.py`: `building_type_code`/`tags`/`bldg_dong` 컬럼 추가, `building_age` 연도
    하드코딩(2026) 버그 수정. `db.py`에 컬럼 추가 + 기존 Supabase 테이블 ALTER 적용 완료.
  - `build_integrated.py`: `naver_listings` 조회에 `building_type_code='OPST'` 필터 추가
    — 안 했으면 멀티타입 데이터 유입 시 삼삼(오피스텔/원룸)이 아파트·상가와 잘못 매칭될 뻔.
  - `subway.py`: 역명에 "역" 접미사 추가(언주 → 언주역).
- **SCHEMA.md 다중타입 반영**: 강남구 샘플(타입별 1건 → 100건 규모)로 실제 API 응답 확인해서
  단지형(아파트/오피스텔, `hscpNo` 있음)과 비단지형(빌라/원룸/단독다가구/상가, `hscpNo` 없음)의
  컬럼 채움 차이를 문서화. 도중에 1건짜리 샘플로 잘못 적었던 "비단지형은 sameAddrCnt 항상 1"을
  100건 검증으로 정정(실제론 비단지형도 지번 기준으로 2건 이상 잡히는 경우 있음).
- `lab/` 신설: 운영 파이프라인에 합치기 전 실험 공간, DB 안 건드리고 로컬 jsonl만 사용.
  목록/상세를 별도 파일로 쪼개지 않고 `naver_listings` 스키마 그대로인 jsonl 하나로 통일하는
  원칙 정리(`lab/README.md`). 강남구 6타입 전수 수집 스크립트(`crawl_naver_gangnam.py`) 작성,
  실행 중(완료되면 `naver_listings_gangnam.jsonl` 커밋 예정).
- **강남 매물 뷰어 신설**(`web/gangnam_app.py` + `templates/gangnam.html`, 포트 5002):
  `lab/naver_listings_gangnam.jsonl`(10,363건·66컬럼)을 DB 없이 그대로 읽어 카드 그리드 +
  상세 모달로 시각화. 기존 `profit.html` 디자인 톤 차용.
  - 통계카드(총건수/동수/월세중앙값/타입분포) + 필터(타입 복수선택·동·키워드·보증금/월세/평수
    범위·정렬) + 카드(타입색배지·건물명·가격·스펙칩·최근접역·한줄소개).
  - 카드 클릭 → 66컬럼을 8그룹(가격/면적/위치/지하철/건물단지/학교/중개사/기타)으로 묶어
    값 있는 것만 표시 + 네이버 원문 링크. 비단지형은 단지정보 그룹이 자동 누락돼 깔끔.
  - 검증: filter/sort/keyword/paging 정상(OPST 역삼동 1,034건, 키워드 "래미안" 521건),
    headless 스크린샷으로 렌더 확인. jsonl이 입력원이라 데이터 갱신 시 재실행만 하면 됨.
- **삼삼 파이프라인 합류 + 매칭 개선** (`feature/samsam-pipeline` → `feature/samsam-matching`):
  - 미머지로 떠있던 Soojung의 삼삼 크롤러(`pipeline/samsam/crawler.py`, 전국 6타입 →
    `samsam_listings`)·DDL·`test_match.py`를 main에 합치고(머지 충돌 3건: db.py/diary/
    build_integrated 수동 해결), 그 위에 매칭 로직을 개선.
  - **build_integrated 재설계**(모듈을 `build_rows`/`write_csv`/`main`으로 분리 → import 시
    DB 안 건드려서 단위테스트 가능):
    - **전월세 환산으로 보증금 정규화**: 보증금 큰 네이버 매물이 월세 낮아 순수익 과대평가되는 문제 해결.
      `환산월세 = 월세 + 보증금(만원)×6%/12` 로 모든 매물을 동일 월비용 기준화. 하드캡(보증금≤2천) 제거,
      대신 `--max-deposit` 선택 옵션으로 남김. 순수익 = 1달실현 − (환산월세+관리비).
    - **같은 오피스텔 삼삼 매물 수**(`삼삼동일건물매물수`) 추가: 건물명+동(없으면 좌표~11m)으로 묶어
      카운트. 기존 `동삼삼매물수`(동 단위)와 구분 — "이 건물에 삼삼 몇 개 떠있나".
    - 네이버 매칭을 `building_type_code='OPST'`로 제한(아파트/상가 오매칭 방지). CSV에 네이버보증금·
      환산월세 컬럼 노출.
    - 검증: psycopg2 stub으로 합성데이터 단위테스트 — 환산월세(1억/30→80, 1천/80→85), 같은건물
      카운트(3/1), 보증금캡 없이 전체매칭, `--max-deposit` 필터 전부 통과.
  - `test_match.py`도 OPST 필터 반영. SCHEMA.md에 `samsam_listings` 실제 컬럼(blocked_days_1m,
    sido/sigungu/dong) + 통합 매칭 규칙(전월세 환산·OPST·동일건물카운트) 명문화.
  - ⚠️ 이 환경엔 .env/DATABASE_URL/psycopg2 없어 실제 전국 수집·적재·e2e 매칭은 미실행
    (DB 연결 환경에서 `crawler.py` → `build_integrated.py` 순으로 돌려야 함).

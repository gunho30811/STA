# 2026-07-14

## 카카오 OAuth UI 개선 + 버그 수정

- `web/auth.py`: 로그인 페이지 UI 개선 — 카카오 버튼 상단 Primary로 올리고 "또는" 구분선 추가.
  회원가입 페이지에서 카카오 버튼 제거(로그인 페이지에서 신규 가입도 처리되므로 불필요).
  카카오 콜백에서 닉네임 갱신 로직 추가(기존 `properties.nickname` 폴백 + 재로그인 시 업데이트).
  `SECRET_KEY` 빈 값 방어 (`or` 폴백).
- `web/portal.py`: `use_reloader=True` — 파일 저장 시 자동 리로드, 서버 재시작 불필요.

## UI 일관성 개선 (tweak/ui-polish)

- Pretendard CDN `<link>` 추가 — auth 페이지(PAGE 템플릿)와 portal 랜딩 둘 다. CSS 선언만 있고 실제 로드 안 되던 문제 수정.
- `auth.py` PAGE CSS: `.kakao-btn` / `.or-sep` 클래스 추가 → 로그인 페이지 인라인 스타일 제거.
  `input` focus ring, `font-family:inherit` 추가. `.btn` margin-top 통일(16px).
- nav 모바일 breakpoint 760px → 640px 통일(다른 모든 요소와 동일하게).
- `portal.py` LANDING `<title>` 문구 통일(`렌트Up · 단기임대 분석`).

## 카카오 OAuth redirect_uri 동적 생성 (fix/kakao-redirect-uri)

- `web/auth.py`: `kakao_login` / `kakao_callback` 에서 `KAKAO_REDIRECT_URI` 환경변수 대신
  `url_for("auth.kakao_callback", _external=True)` 사용 — 접속 호스트 기준으로 자동 생성.
  Vercel 배포 환경에서 폰으로 접속 시 `localhost` 가 redirect_uri에 박혀 KOE101 나던 문제 수정.
- `web/portal.py`: `ProxyFix(x_proto=1, x_host=1)` 추가.
- `web/auth.py`: redirect_uri를 `_KAKAO_REDIRECT_URI or url_for(...)` 방식으로 변경 —
  Vercel 환경변수에 명시적으로 설정된 값을 우선 사용, 없으면 url_for 폴백.
  Vercel env var `KAKAO_REDIRECT_URI=https://sta-gray.vercel.app/auth/kakao/callback` 필요.

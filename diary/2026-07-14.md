# 2026-07-14

## 카카오 OAuth UI 개선 + 버그 수정

- `web/auth.py`: 로그인 페이지 UI 개선 — 카카오 버튼 상단 Primary로 올리고 "또는" 구분선 추가.
  회원가입 페이지에서 카카오 버튼 제거(로그인 페이지에서 신규 가입도 처리되므로 불필요).
  카카오 콜백에서 닉네임 갱신 로직 추가(기존 `properties.nickname` 폴백 + 재로그인 시 업데이트).
  `SECRET_KEY` 빈 값 방어 (`or` 폴백).
- `web/portal.py`: `use_reloader=True` — 파일 저장 시 자동 리로드, 서버 재시작 불필요.

## UI 일관성 개선 (tweak/ui-polish)

- Pretendard CDN `<link>` 추가 — auth 페이지(PAGE 템플릿)와 portal 랜딩 둘 다. CSS 선언만 있고 실제 로드 안 되던 문제 수정.
- `auth.py` PAGE CSS: `.kakao-btn` / `.or-sep` 클래스 추가 → 로그인 페이지 인라인 스타일 제거.
  `input` focus ring, `font-family:inherit` 추가. `.btn` margin-top 통일(16px).
- nav 모바일 breakpoint 760px → 640px 통일(다른 모든 요소와 동일하게).
- `portal.py` LANDING `<title>` 문구 통일(`렌트Up · 단기임대 분석`).

## 카카오 OAuth redirect_uri 동적 생성 (fix/kakao-redirect-uri)

- `web/auth.py`: `kakao_login` / `kakao_callback` 에서 `KAKAO_REDIRECT_URI` 환경변수 대신
  `url_for("auth.kakao_callback", _external=True)` 사용 — 접속 호스트 기준으로 자동 생성.
  Vercel 배포 환경에서 폰으로 접속 시 `localhost` 가 redirect_uri에 박혀 KOE101 나던 문제 수정.
- `web/portal.py`: `ProxyFix(x_proto=1, x_host=1)` 추가.
- `web/auth.py`: redirect_uri를 `_KAKAO_REDIRECT_URI or url_for(...)` 방식으로 변경 —
  Vercel 환경변수에 명시적으로 설정된 값을 우선 사용, 없으면 url_for 폴백.
  Vercel env var `KAKAO_REDIRECT_URI=https://sta-gray.vercel.app/auth/kakao/callback` 필요.

## 카카오 로그인 KOE101 원인 = 재배포 누락 + 카카오 회원도 관리자 승인 게이트 (fix/kakao-admin-approval)

- **KOE101 진단**: 프로덕션 `/auth/kakao`가 카카오로 보내는 URL의 `client_id=`가 비어 있던 것이 원인
  (redirect_uri는 정상이었음). Vercel 환경변수에 `KAKAO_CLIENT_ID`는 저장돼 있었으나 **저장 후
  재배포를 안 해** 예전 배포(빈 값)가 떠 있었음. 빈 커밋 push로 재배포 → `client_id` 채워짐 확인, KOE101 해소.
  (로컬 `.env`엔 카카오 키가 없어 로컬 서버는 여전히 카카오 미동작 — 필요 시 별도 주입.)
- **승인 게이트 누락 수정**: 신규 카카오 회원이 `approved=TRUE`로 자동 생성 + 즉시 세션 로그인돼,
  이메일 가입(관리자 승인 대기)과 정책이 어긋났음. `kakao_callback`을 수정:
  - 신규 INSERT를 `approved=FALSE`로(카카오 인증은 email_verified만 대체, 관리자 승인은 필요).
  - 세션 부여 전 `role/approved` 확인 — 미승인 일반 회원은 로그인 대신 '가입 완료·관리자 승인 후
    로그인' 안내 페이지 반환(이메일 가입과 동일 UX). admin/이미 승인된 회원만 로그인.
  - 기존에 이미 approved=TRUE로 만들어진 카카오 회원은 그대로 로그인 유지(신규만 게이트).
- 검증: mock으로 3경로(신규 가입=승인대기·세션없음 / 승인된 회원=로그인 302 / 미승인 재시도=대기) 통과.

## 브랜드 리뉴얼: 렌트Up → rendit (로고 반영 + 퍼플 테마) (feature/rebrand-rendit)

- 사용자가 새 로고(`rendit.png`) 제공 — 소문자 워드마크, "ren"(잉크) + "dit"(퍼플) 2톤, 둥근 기하학 산세리프.
  .NET System.Drawing으로 픽셀 색 추출: 잉크 `#171A23`, 퍼플 `#4321F3`.
- **브랜드명 렌트Up → rendit**: 네비 로고·랜딩·로그인 카드·대시보드 제목·브라우저 탭 제목(main.jsx 4개).
  다크 배경(네비/랜딩)은 ren=화이트·dit=`#8b7dff`, 라이트 배경(로그인)은 ren=`#171A23`·dit=`#4321F3`.
  옛 상승막대(///) SVG는 제거(실제 로고가 순수 워드마크).
- **강조색 파랑 → 퍼플**: `#2563eb`(38곳)→`#4321F3`, hover `#1d4ed8`→`#3517c4`,
  브랜드 라이트 `#60a5fa`→`#8b7dff`, glow/tint `rgba(37,99,235,*)`→`rgba(67,33,243,*)`.
  web/auth.py·portal.py + 프론트 4뷰어 CSS/JSX. `npm run build`로 dist 재빌드.
- **카드 아이콘 이모지 → rendit 타일**: 랜딩·대시보드 3카드(수익성/렌트 데이터/부동산 매물)의
  💰🛋️🏙️ 이모지를 퍼플 라운드 사각 타일 + 흰 글리프(우상향 그래프/예약 캘린더/건물) SVG로 교체
  (`portal.py` `_tile()` 헬퍼, `{ICON_*}` 플레이스홀더를 `.replace`로 주입).
- '렌트'(단기임대 데이터 용어: 렌트 분석/렌트 데이터/렌트 계정)는 브랜드명이 아니라 그대로 유지.
- 검증: 랜딩/로그인/수익성 뷰어 스크린샷으로 로고 톤 일치 확인, py_compile·빌드 통과.
- TODO(후속): GPT 제작 실로고 파일 오면 CSS 워드마크 → 실제 이미지 교체 + 파비콘/앱아이콘 적용.

## 파비콘 추가 (feature/favicon-rendit)

- 그동안 파비콘이 아예 없었음(`/favicon.ico`는 로그인 게이트로 302, HTML에 icon link 0).
- rendit 로고 톤의 파비콘 제작: 퍼플(#4321F3) 라운드 사각 타일 + 흰 'r'(둥근 기하학). 16px에서도
  또렷하게 'r'로 읽히는 것 렌더 확인.
- **data URI(base64 SVG)로 각 `<head>`에 직접 삽입** — 파일 서빙은 로그인 게이트에 걸려 302되므로.
  `_inject_nav`(로그인 후만 동작)에 안 얹고, 미로그인 랜딩·로그인도 뜨게 4곳에 넣음:
  auth.py PAGE / portal.py LANDING·PUBLIC_LANDING / frontend/index.html.
- 덤: `frontend/index.html`의 옛 `<title>`("삼삼 × 부동산 단기임대 수익성") → "rendit"로 수정
  (런타임엔 main.jsx가 덮지만 정적 기본값도 정리).
- 프론트 재빌드, 랜딩/로그인/뷰어 전 페이지 favicon link 확인, base64→SVG 디코드 검증.
- 피드백("좀 더 둥글게"): 첫 버전은 팔이 오른쪽으로 길어 어색 → 변형 5종 렌더 비교 후
  통통·둥근 버전(rx30·stroke18·짧은 팔)으로 교체. 4곳 base64 일괄 치환 + dist 재빌드.
- 재피드백("네모→동그라미였고 r이 찐따같다 / 헤더 위아래 안맞음 / 로고 반응형"): 3건 수정.
  1. 파비콘: **네모 타일 → 완전 원형(circle)** + r을 또렷하게(stroke13, 표준 비율). 원형+r 4종
     렌더 비교 후 V4 채택. 16px에서도 'r' 판독.
  2. 대시보드 헤더 중복 제거: 상단 주입 네비바가 이미 rendit·메뉴·로그아웃을 갖는데 대시보드가
     'rendit 단기임대 수익성 분석 + 관리자님 로그아웃'을 또 반복 → 위아래 정렬 깨지고 지저분.
     → 대시보드 h1을 '{이름}님, 환영합니다 👋' 인사말로 교체하고 중복 로그아웃 제거(.who).
  3. 반응형: 안 줄던 큰 h1(브랜드+부제)이 모바일 겹침 원인이었음 → 위 인사말 단순화로 해결.
     (네비·랜딩 브랜드는 이미 반응형/햄버거.) 데스크탑·모바일 재캡처로 정렬·반응형 검증.

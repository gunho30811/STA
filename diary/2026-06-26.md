# 2026-06-26

- **삼삼 수익성 뷰어(`web/profit_app.py` + `templates/profit.html`)에 매칭 개선 컬럼 반영**:
  전날 build_integrated에 추가한 `삼삼동일건물매물수`(건물삼삼수)·`네이버환산월세_만원`(환산월세)을
  뷰어 표에 노출. 순수익/현실효율 정의가 환산월세 기반으로 바뀐 것도 범례에 반영.
  - `profit_app.py`: `PROFIT_MAP`에 신규 컬럼 + 구·신 헤더 동시 매핑(현실효율/순수익은 환산 도입으로
    헤더 문구가 바뀜). `load_profit`에서 키를 먼저 기본값 초기화 후 **CSV에 실제 있는 컬럼만** 채우도록
    수정 — 안 그러면 같은 키에 매핑된 없는 헤더가 값을 None으로 덮어쓰는 버그.
  - `profit.html`: COLS·행 셀에 건물삼삼수·환산월세 추가(헤더↔셀 위치 수동 동기화), 범례 갱신
    (환산월세=월세+보증금×6%/12, 순수익=1달실현−네이버월총, 건물삼삼수=같은 건물 삼삼 매물수).
  - 검증: 현재 디스크 CSV는 구버전(569건, 신컬럼 없음)인데도 정상 로드 — 신컬럼은 '-'로 안전 표시,
    기존 profit/realEff 값 보존 확인. headless 스크린샷으로 표 정렬 OK.
  - DB 환경에서 `build_integrated.py` 재실행하면 신컬럼이 실제 값으로 채워짐(뷰어는 코드 변경 불필요).
- **DB 실연결(pg8000) + 삼삼 크롤러 대수술 + 옵션 공실/예약률 뷰어**:
  - `.env`에 DATABASE_URL/SAMSAM 크리덴셜 들어옴. 근데 Python 3.15 알파라 psycopg2 휠이 없어 빌드 실패
    → 순수파이썬 **pg8000** 설치 후 `db.py`에 폴백 추가(psycopg2 우선, 없으면 pg8000). Supabase 풀러는
    자체 CA라 공개검증 불가 → 사용자 승인 하에 암호화+CA검증생략(기존 psycopg2 sslmode와 동일,
    `DB_SSL_CA` 주면 정식 검증). `_Row`도 pg8000 description(튜플) 호환.
  - **네이버 강남 jsonl(10,363) → DB `naver_listings` 적재** (사용자 요청, 매칭용 실데이터 확보).
  - **삼삼 크롤러 버그 다수 수정**: 실제 API와 키가 안 맞아 그대로 돌리면 거의 빈 행이 나올 상태였음
    (samsam_listings 0건 = 한 번도 성공 못 함). 실 API 탐침으로 정정:
    - 목록 API가 지역(state/province/town)·좌표·주소·가격(usingFee/mgmtFee)·평수·방수까지 줘서
      `collect_rids`가 room 전체를 보관 → **지역 사전필터**(강남만 상세 호출, 전국 상세 안 받음).
    - 상세 키 정정: 면적 `squareMeterSize`, 옵션 `additionalOptions`(←`extraOptions` 오타), 엘베
      `hasElevator`, 주차 `parkingType`. 옵션값은 영문 코드(TV/REFRIGERATOR…).
    - 스케줄: `year`+`month`(정수) 필수, 응답 `data.schedules`=리스트 → {날짜:상태} 병합.
      상태 어휘 확정: `booking`(예약)/`disable`(막힘).
    - 검증: 강남 실매물 1건 map_row → 지역/건물명/면적/주세/예약일수(23/44/63)/옵션 전부 정상.
  - **옵션 공실/예약률 뷰어**(`web/samsam_app.py` + `templates/samsam.html`, 포트 5003):
    "이 옵션 없어도 잘 나갈까?" — 지역/유형 거른 뒤 옵션별 **있는 집 vs 없는 집 평균 예약률** 비교표
    (차이 큰=중요, 작거나 음수=없어도 됨). 옵션 클릭 시 그 옵션 없는 집 목록(공실 높은 순). 예약률=
    예약일/(30−막힘일). DB 우선, 비면 합성샘플(`lab/samsam_sample.jsonl`, `make_samsam_sample.py`) 폴백.
    옵션코드→한글은 `OPTION_KO`.
  - 강남 삼삼 크롤 백그라운드 실행(전국 15,700 → 강남 1,019건 적재 완료, 실패 1). 뷰어 재시작 시 DB 전환.
- **교란 보정(같은 평수·가격대 비교) 추가**: 단순 전체평균 옵션 비교는 "옵션 없는 집이 우연히 더 크거나
  비싼" 교란에 취약(실제로 강남 TV없음=12.1평·주당103만 vs TV있음=10.6평·71만, 욕조는 단순 −22.9였는데
  사실 큰·고가 매물 편향). → `_adj_diff`: 평수 2평 + 주당 10만원 같은 칸에서만 옵션 유무 예약률 차이를
  가중평균한 **보정차이** 컬럼 추가. 평수/주당 범위 필터 + 그룹별 평균 평수·주당 노출(교란 눈으로 확인).
  결과: TV 보정 +10.8%p(표본35), 욕조 −22.9→+8.6(부호 역전), 식탁 +13.7(표본102)·책상 +7.2(표본203)
  처럼 표본 충분한 게 신뢰 가능.
- **판정(필수재/측정가능/표본부족) 추가** — "에어컨·냉장고·침대는 당연히 중요한데 보정값이 이상하다"는
  피드백 대응. 원인: 강남 오피스텔은 냉장고 99.7%·에어컨 99.1%·침대 97.9% 보유 → "없는 집"이 2~14채뿐
  이라 단순/보정 둘 다 못 믿고 마구 튐(흔들림 자체가 측정불가 신호). 보유율로 분류:
  - 보유율 ≥95% = **사실상 필수**(효과 데이터로 측정 불가, 그냥 갖춰야 함) → 에어컨/냉장고/침대/세탁기/
    도어락/와이파이가 여기로 정확히 잡힘(직관과 일치).
  - 없는집 ≥20 & 보정표본 ≥15 = **측정가능**(보정차이 신뢰) → 식탁/책상/CCTV/소파/TV 등.
  - 그 외 **표본부족**. `보유율` 컬럼 추가 + 판정별 정렬/색.
- **매물 목록 헤더 정렬 추가**: 옵션 클릭 후 뜨는 "없는 집" 목록을 평/주당/예약일/막힘/예약률/공실률
  헤더 클릭으로 오름·내림 정렬(기본 공실률 높은 순). 일일이 눈으로 안 봐도 정렬로 확인 가능.
- **통합 수익성 뷰어(`profit_app`, 5001) 실데이터 갱신 + 전면 개편**:
  - 새 매칭 파이프라인(`build_integrated.py`, DB) 실행 → 강남 매칭 **429건**으로 CSV 재생성.
  - **용어/지표 정리**(임대인 피드백): ① 1달실현 → **최대수익**(삼삼 월환산=풀가동 월매출), 순수익=
    최대수익−네이버월총 ② **공실률 → 예약률**로 전면 교체(높을수록 좋음, 거꾸로 안 헷갈리게)
    ③ **구공실률·구경쟁 제거 → 동예약률·동경쟁**(더 작은 단위) ④ **현실효율 삭제**.
  - **마스터-디테일 UI**: 왼쪽 간결 표(매물/동/평/최대수익/순수익/예약률) + 행 클릭 시 오른쪽 상세
    패널(가로 스크롤 제거). 조건 필터(순수익/최대수익/예약률/동예약률/평수/보증금/방수/키워드).
  - **순위 탭 신설**: 동별·역별 평균 순수익/예약률/최대수익 랭킹(어디서 운영이 제일 유리한지).
  - 동/역 예약률은 통합 행에서 직접 집계(구 단위 CSV 의존 제거). 참고: 최대수익 기준이라 예약 0인
    대형 매물이 순수익 상위에 뜰 수 있음 → 예약률 필터로 실수요 매물 선별 가능.
- **예약률 100% 초과 버그 수정**: 수집 윈도우가 오늘~+30일=31일(양끝 포함)이라 예약일이 최대 31인데
  분모를 30으로 나눠 100% 초과 발생(355/1019건). 분모를 31−막힘일로 바로잡음(예약일+막힘일 ≤31이라
  ≤100% 보장) + 100 상한 클램프. profit_app/samsam_app/build_integrated 모두 적용.
- **건물(오피스텔) 인기 순위 기능**(samsam_app `/api/buildings` + samsam.html "건물 인기" 탭):
  "한 건물에 삼삼 여러 채가 다 잘 나가면 검증된 대박 건물"이라는 임대인 아이디어. 건물명+동으로 묶어
  매물수·평균예약률·**최저예약률**(전 호실 다 잘 나가는지)·최고·평균주당 집계, 매물수≥N 필터.
  예: 로뎀빌B(역삼,4채 평균99%·최저97%)=대박, 테헤란로 대우아이빌(11채 평균88%인데 최저0%)=한 호실 죽음.
- **전국 삼삼 크롤 백그라운드 실행**(강남 1,019 skip, 나머지 ~14,700건 상세 수집 → DB 자동 적재).
- **네이버 전국 상세 크롤 시작**: 목록(`listings`)은 이미 전국 36,569건 있고 상세(`naver_listings`)만
  강남 1만뿐 → `crawl_detail.py`에 `--sleep` 옵션 추가하고 0.9s로 조심히 백그라운드 실행(~26k, 이어받기).
  네이버/삼삼은 다른 사이트라 동시 실행해도 사이트별 차단율은 안 오름.
- **예약률 기준 명확화 + 주간 스냅샷 기반**:
  - 예약률 = "수집일(collected_at) 기준 향후 30일"의 예약 비율(5월/6월 고정 아님, 롤링). samsam 뷰어
    헤더에 "수집일(날짜)~향후 30일(~날짜)" 명시(`/api/facets` occ_window).
  - 매주 전체 예약률을 쌓아 지역 인기 트렌드를 추적하기 위한 토대: `samsam_snapshots` 테이블(스냅샷날짜×
    시도·시군구·동·유형 → n·avg_occ_1m·avg_occ_3m·avg_week) + `pipeline/samsam/snapshot.py`(크롤 직후
    1회 실행해 집계 적재). 전국 크롤 완료 후/매주 실행 예정.
  - **지역 트렌드 뷰**(samsam_app `/api/trend` + samsam.html "지역 트렌드" 탭): 스냅샷을 동별로 묶어
    주차별 예약률 + 전주대비 Δ(▲상승/▼하락) 표시. 스냅샷 1개면 현재 랭킹만, 2주차부터 변화 표시.
    첫 스냅샷(2026-06-26) 적재해 검증(강남 수서동 64%·일원동 60%… 랭킹 정상).

# 2026-06-26

- **삼삼 수익성 뷰어(`web/profit_app.py` + `templates/profit.html`)에 매칭 개선 컬럼 반영**:
  전날 build_integrated에 추가한 `삼삼동일건물매물수`(건물삼삼수)·`네이버환산월세_만원`(환산월세)을
  뷰어 표에 노출. 순수익/현실효율 정의가 환산월세 기반으로 바뀐 것도 범례에 반영.
  - `profit_app.py`: `PROFIT_MAP`에 신규 컬럼 + 구·신 헤더 동시 매핑(현실효율/순수익은 환산 도입으로
    헤더 문구가 바뀜). `load_profit`에서 키를 먼저 기본값 초기화 후 **CSV에 실제 있는 컬럼만** 채우도록
    수정 — 안 그러면 같은 키에 매핑된 없는 헤더가 값을 None으로 덮어쓰는 버그.
  - `profit.html`: COLS·행 셀에 건물삼삼수·환산월세 추가(헤더↔셀 위치 수동 동기화), 범례 갱신
    (환산월세=월세+보증금×6%/12, 순수익=1달실현−네이버월총, 건물삼삼수=같은 건물 삼삼 매물수).
  - 검증: 현재 디스크 CSV는 구버전(569건, 신컬럼 없음)인데도 정상 로드 — 신컬럼은 '-'로 안전 표시,
    기존 profit/realEff 값 보존 확인. headless 스크린샷으로 표 정렬 OK.
  - DB 환경에서 `build_integrated.py` 재실행하면 신컬럼이 실제 값으로 채워짐(뷰어는 코드 변경 불필요).

# 2026-06-23

- PR #18 머지본(`data/*.db`·`*.jsonl` git 추적 제거, close #5) main에 fast-forward로 땡겨옴.
- **수집원별 협업 분리**: 네이버(gunho) / 삼삼(Soojung) 두 사람이 충돌 없이 작업하도록
  `pipeline/`을 소스별 폴더로 재구성.
  - `pipeline/naver/` ← crawler.py, get_targets.py, crawl_nonseoul.py, enrich_nonseoul.py
  - `pipeline/samsam/` ← classify_btype.py
  - `pipeline/integrate/` ← build_integrated.py(+sqlite 레거시, migrate_to_supabase 1회성)
  - `git mv`로 이동(이력 보존), 파일이 한 단계 깊어져 경로 계산(`dirname` 체인)에 한 단계씩 추가.
    8개 파일 전부 루트 해석 OK + `py_compile` 통과 확인. README 폴더구조/실행경로 갱신.
- **스키마 계약 문서 `SCHEMA.md` 신규 작성**: 각 수집원이 자기 테이블에만 write,
  통합 단계는 읽기만 하는 인터페이스를 명문화(계약 우선 → 코드 변경).
  - `samsam_listings`: 매물명/도로명·지번·층/면적/방·화장실·주방·거실/엘베·주차/기본·추가옵션(jsonb)/
    주당 임대료·관리비·총합/1·2·3달 예약일수/인근역 500m·1km count+역명.
  - `naver_listings`: 보증금·월세·관리비/계약·전용면적/층/방·욕실/향·복층/지번·도로명·건물명/단지정보
    (용도·승인일·세대수·주차·용적률 등)/시설(jsonb)/중개사(상호·전화·등록번호·집주인확인)/중개보수/
    학교/지하철/같은 건물 같은 면적 매물수.
- 협업 충돌 방지 원칙 정리: 폴더로 파일 소유권 분리 + 통합지점은 테이블 계약으로 고정 +
  생성물(CSV/DB) git 미추적 + 작은 PR 자주 머지.
- **네이버 상세 데이터 출처 규명** (샘플 1건: 역삼동 상지카일룸블랙 2632661141):
  - 매물당 API 3개 묶으면 SCHEMA 대부분 채워짐: `articles/{no}`(가격·면적·시설·현관구조·중개사·summary) +
    `complexes/{c}`(도로명·지번·용적률·건폐율·세대·주차·건설사) + `complexes/{c}/schools`(배정초교).
  - **지번 주소가 단지 API에 있음**(역삼동 653-3) — diary의 "네이버 지번 미제공"은 매물/목록 API 한정이었음.
  - `same_building_same_area_count` = 매물탭 `articles/complex/{c}` 의 `sameAddrCnt`(평형별로 따로 잡힘, 확인).
  - **지하철 역명/거리(m)는 네이버 JSON에 없음**(도보 분만). 신플랫폼 fin.land엔 있으나 로그인 전용 front-api라
    별도 인증 필요 → **좌표 기반 자체 계산**으로 해결: 수도권 역좌표표 `data/subway_stations.csv`(589역) +
    `pipeline/naver/subway.py`(haversine 최근접역). 샘플 결과 **언주역 406m**(광고문구 "언주역 더블역세권"과 일치).
- **네이버 상세 수집기 작성** (`pipeline/naver/`): listings(목록)를 상세 API 3종 + 좌표 역계산으로
  보강해 `naver_listings`(SCHEMA 62컬럼)에 적재.
  - `detail_map.py`: 상세/단지/학교 응답 → 행 매핑(순수 함수, db·네트워크 비의존). 층은 articleFloor
    없으면 `floorInfo` "현재/총" fallback.
  - `crawl_detail.py`: crawler.NaverLand 재사용, 매물당 articles/{no}+complexes/{c}+/schools 호출,
    이어받기(이미 적재분 skip), 50건 배치 upsert.
  - `db.py`: `naver_listings` 테이블 DDL(63컬럼=62+crawled_at)+인덱스+PK 추가. JSON컬럼은 TEXT(json).
  - 검증: 순수매핑 62컬럼 일치, 라이브 e2e(강남·분당 각 2건) 60~61/62 채움, DDL↔INSERT 63컬럼 일치.
  - ⚠️ 실제 Supabase 적재는 미실행(이 환경에 psycopg2 빌드 불가 + DATABASE_URL 없음).
    DB 연결되는 환경에서 `python pipeline/naver/crawl_detail.py --limit 20` 으로 적재 확인 필요.

## 세션 2 (Soojung)
- **naver_listings Supabase 적재 확인**: `python db.py`로 테이블 생성 후
  `crawl_detail.py --limit 20` 테스트 → 20건 중 11건 적재(9건은 삭제된 매물), 건물명·지번·역명 정상.
- **`build_integrated.py` naver_listings 기반으로 전환**:
  - `load_nav()`: `listings` → `naver_listings`, 컬럼 `articleno/articlename/area_m2/rent/lon/mgmt/floorinfo`
    → `article_no/building_name/area_exclusive_m2/rent_monthly/lng/maintenance_monthly/floor_current`
  - `_parse_nav_floor`·`_floor_category` 제거 (floor_current 정수 직접 사용), `_floor_ok` 단순화
  - 관리비 단위 처리 단순화 (`man()` 불필요 — maintenance_monthly 이미 만원)
- **SCHEMA.md 수정**: `school_distance_m` → `school_walk_min` + `school_student_per_teacher` (실제 코드 기준)
- **GitHub Actions 자동화** (`.github/workflows/crawl.yml`):
  매주 월요일 10:00 KST + 수동 트리거. 목록 크롤 → 상세 크롤(신규분만) → CSV 재생성 → 변경분 커밋.

## 세션 마무리
- 위 작업을 `feature/split-pipeline-and-schema` 브랜치에 커밋(6개)하고 **PR #19**로 main에 머지.
  - 흐름: PR #18 땡김 → pipeline 소스별 분리 + SCHEMA.md 계약 → SCHEMA 세부(같은면적 1평/summary 필수) →
    네이버 상세 데이터 출처 규명(상세+단지+학교 API, 지번은 단지 API에 있음) →
    지하철 좌표 역계산(`data/subway_stations.csv` 수도권 589역, `subway.py`) →
    상세 수집기(`crawl_detail.py`/`detail_map.py` + `naver_listings` 테이블).
- 다음 후보: ① 실제 Supabase 적재 테스트(.env 필요) ② 뷰어(`web/`)가 `naver_listings` 읽도록 연결
  ③ 삼삼 쪽(`pipeline/samsam/`) 동일 패턴으로 `samsam_listings` 수집(Soojung).

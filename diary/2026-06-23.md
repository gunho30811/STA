# 2026-06-23

- PR #18 머지본(`data/*.db`·`*.jsonl` git 추적 제거, close #5) main에 fast-forward로 땡겨옴.
- **수집원별 협업 분리**: 네이버(gunho) / 삼삼(Soojung) 두 사람이 충돌 없이 작업하도록
  `pipeline/`을 소스별 폴더로 재구성.
  - `pipeline/naver/` ← crawler.py, get_targets.py, crawl_nonseoul.py, enrich_nonseoul.py
  - `pipeline/samsam/` ← classify_btype.py
  - `pipeline/integrate/` ← build_integrated.py(+sqlite 레거시, migrate_to_supabase 1회성)
  - `git mv`로 이동(이력 보존), 파일이 한 단계 깊어져 경로 계산(`dirname` 체인)에 한 단계씩 추가.
    8개 파일 전부 루트 해석 OK + `py_compile` 통과 확인. README 폴더구조/실행경로 갱신.
- **스키마 계약 문서 `SCHEMA.md` 신규 작성**: 각 수집원이 자기 테이블에만 write,
  통합 단계는 읽기만 하는 인터페이스를 명문화(계약 우선 → 코드 변경).
  - `samsam_listings`: 매물명/도로명·지번·층/면적/방·화장실·주방·거실/엘베·주차/기본·추가옵션(jsonb)/
    주당 임대료·관리비·총합/1·2·3달 예약일수/인근역 500m·1km count+역명.
  - `naver_listings`: 보증금·월세·관리비/계약·전용면적/층/방·욕실/향·복층/지번·도로명·건물명/단지정보
    (용도·승인일·세대수·주차·용적률 등)/시설(jsonb)/중개사(상호·전화·등록번호·집주인확인)/중개보수/
    학교/지하철/같은 건물 같은 면적 매물수.
- 협업 충돌 방지 원칙 정리: 폴더로 파일 소유권 분리 + 통합지점은 테이블 계약으로 고정 +
  생성물(CSV/DB) git 미추적 + 작은 PR 자주 머지.
- **네이버 상세 데이터 출처 규명** (샘플 1건: 역삼동 상지카일룸블랙 2632661141):
  - 매물당 API 3개 묶으면 SCHEMA 대부분 채워짐: `articles/{no}`(가격·면적·시설·현관구조·중개사·summary) +
    `complexes/{c}`(도로명·지번·용적률·건폐율·세대·주차·건설사) + `complexes/{c}/schools`(배정초교).
  - **지번 주소가 단지 API에 있음**(역삼동 653-3) — diary의 "네이버 지번 미제공"은 매물/목록 API 한정이었음.
  - `same_building_same_area_count` = 매물탭 `articles/complex/{c}` 의 `sameAddrCnt`(평형별로 따로 잡힘, 확인).
  - **지하철 역명/거리(m)는 네이버 JSON에 없음**(도보 분만). 신플랫폼 fin.land엔 있으나 로그인 전용 front-api라
    별도 인증 필요 → **좌표 기반 자체 계산**으로 해결: 수도권 역좌표표 `data/subway_stations.csv`(589역) +
    `pipeline/naver/subway.py`(haversine 최근접역). 샘플 결과 **언주역 406m**(광고문구 "언주역 더블역세권"과 일치).

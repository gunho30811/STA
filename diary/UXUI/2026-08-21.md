# 2026-08-21

- (Eunji-Gil) `docs/design-system.md` 작성 — frontend 4개 뷰어 CSS + web/portal.py·map_view.py·
  auth.py에 실제로 박혀 있는 색상/font-size/border-radius/box-shadow/spacing 값을 정규식으로
  스캔해서 값별 사용 빈도·위치를 정리한 현황 인벤토리. 판단 없이 현황만.
- (Eunji-Gil) 레퍼런스 스크린샷 5장(삼삼엠투 3장 + 토스 앱 2장)을 육안 측정해서
  `docs/reference-rules.md` 작성 — 배경 레이어 단계, 면 분리 방식(border vs shadow), 폰트
  위계 배율, 브랜드색 면적 비율, 숫자:라벨 배율, spacing 그리드 등 6개 축으로 공통 규칙 추출.
- (Eunji-Gil) 두 문서를 대조해서 `docs/design-system.md`에 금지/강제 규칙 섹션 추가 (spacing
  4px 배수, 폰트 위계 배율, shadow 종수 제한, 브랜드색 면적 제한 등).
- (Eunji-Gil) 위 규칙을 계산기(`/calc`) **결과 존**에만 적용 — 입력 존·시나리오 표는 그대로
  두고, `vac-num`/`hero-card`/`kpi3-cell`/`gauge-caption`/`gauge2-thumb` 등 결과 존 전용
  클래스만 수정. 폰트 10종→5종, 4px 배수 아닌 spacing 8곳 정리, `gauge2-thumb`의
  border+shadow 동시 적용(택1 위반) 제거. 공유 클래스(`.box`,`.vac-eyebrow` 등)는 직접 안
  건드리고 `.vac-head .vac-eyebrow{...}` 식으로 스코프해서 입력 존/시나리오 표에 안 새게 처리.
- (Eunji-Gil) 계산기 수익 계산식 버그 수정 — "예상 수익" 카드의 "청소·관리비" 단일
  필드(`i_wmgmt`)가 청소비까지 관리비처럼 매주 반복되는 걸로(WPM 환산 + 플랫폼수수료·부가세
  적용) 계산되고 있었음. **관리비**(`i_wmgmt`, 주당·수수료 적용)와 **청소비**(신규
  `i_wclean`, 월 1회 고정·수수료 미적용·공실률 무관)로 필드 분리하고 `calc()`의
  `weeklyGross`/`effRevenue`/손익분기(`beVacancyPct`,`beOccupiedRaw`)/시나리오 표 공식을
  전부 맞춰 조정. 손익분기는 `fixedNet=totalCost-cleaningMo`로 청소비만큼 미리 빼서 계산.
  프리셋값(관리비/청소비, 주당/월): 원룸 5/5만원, 빌라·주택 9/8만원, 오피스텔 6/5만원.
- (Eunji-Gil) 계산기 결과 존 세부 다듬기 (아직 커밋 전). 예약일수 설정 카드는 숫자를
  좌측 정렬 세로 스택으로 재배치. 카드 제목은 전부 `h2`로 통일(폰트크기 14~20px 정리, 죽은
  CSS `.vac h2{font-size:20px}` 제거). 손익분기·영업이익률 KPI 박스는 상태별 색(골드/초록/
  빨강) 유지하되 영업이익률은 흑자/적자 2단계로 단순화. 예약일수 안내 문구는 적자일 때만
  노출("n일 더 예약되면 흑자로 바뀌어요" + 체크 아이콘)하도록 변경. 그 외 h1 크기, 12.5px→
  12px 통일 등 자잘한 정리.

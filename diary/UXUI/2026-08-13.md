# 2026-08-13

- (Eunji-Gil) 계산기(/calc) 페이지를 Figma Make 디자인대로 리디자인. 처음엔 스크린샷으로
  역산하다가, 사용자가 Figma Make 실제 export 코드(design/culc_redesign/App.tsx)를 줘서
  그걸 기준으로 정확하게 맞춤 — 레이아웃(좌 입력/우 결과 2단), 계산 공식(예약일수 슬라이더
  기반), 손익분기 게이지, 시나리오 막대그래프, 시장비교 카드, 하단 CTA까지 블록 단위로
  나눠서 커밋.
- 버그 수정: 공실을 적자 구간으로 옮기면 hero-card가 세로로 늘어나던 문제 — 금액에 쓰던
  유니코드 마이너스(−)가 폰트에 없어 폴백되면서 그 줄만 줄높이가 커진 것. ASCII 하이픈으로
  교체해서 해결.
- 세부 다듬기: 포인트 색상은 Figma 값 대신 기존에 쓰던 디자인 토큰으로 복원, 카드
  그림자·명도 대비로 입체감 보강, 툴팁·상태뱃지 디자인 여러 차례 보정, 지역 선택은
  네이티브 select 커스텀이 안 돼서 직접 만든 드롭다운으로 교체.

## TODO — "비슷한 매물과 비교하면" 실데이터 연동 (별도 작업)

계산기 "나는 얼마나 벌고 있을까요?" 섹션(`MARKET_DATA`)이 지금 Figma Make가 만든 목업
숫자(전국/서울/경기 × 원룸, 총 743/412/432건)라서 실제 시장 데이터가 아님. 확인해보니
연동 가능한 진짜 데이터가 있음:

- **`net_profit` 테이블**(Supabase) — `/profit` 페이지(`web/profit_app.py`)가 이미 쓰는 실데이터.
  `sido`/`sigungu`/`dong`(지역), `btype`(매물유형: 오피스텔/원룸/연립빌라/단독주택 등),
  `expNet`(기대 월순수익 = 예약률 반영 실현매출 − 네이버 환산월세총) — 계산기의 "월 순수익"과
  개념이 거의 동일.
- 표본 수: 2026-07-12 일지 기록 기준 **3,829건** (지금 목업보다 큼).
- `profit_app.py`의 `P()`(5분 캐시 메모리 로드) 패턴을 재사용 가능.
- 이 세션에서 DB 직접 접속을 시도했으나 연결 실패(sandbox 네트워크 제약으로 추정, 실제
  배포 환경에선 다른 뷰어가 정상 접속 중이라 문제 없을 가능성 높음 — 실제 작업 시 재확인 필요).

**할 일**: `web/portal.py`(계산기)가 `net_profit`을 읽어와 지역×유형별로 구간화(bucketing)하고
p25/median/p75·상위 N% 계산하는 백엔드 로직 추가 → 지금의 하드코딩 `MARKET_DATA`를 대체.

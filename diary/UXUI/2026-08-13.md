# 2026-08-13

- (Eunji-Gil) 계산기(/calc) 페이지 Figma Make 리디자인 반영 시작. 스크린샷 5장(데스크톱 전체 +
  매물유형별 3장 + 초기화 상태) 분석해서 블록 단위 계획 수립, 블록마다 커밋 + 로컬 확인 후 진행.
  Block 1: 1단 스택 → 좌(입력)/우(결과) 2단 레이아웃 전환, 매물유형 탭·초기화 버튼 마크업 추가.
  계산 로직은 그대로 두고 화면 구조만 변경 — 다음 블록(탭 기본값 전환)부터 로직 작업.
- (Eunji-Gil) design/culc_redesign/에 Figma Make "Export code" 결과물(App.tsx, 실제 React
  소스)을 받아서 스크린샷 역산 대신 정확한 값으로 교체. 카드 스타일(테두리 없이 shadow)·공식
  (WPM=365/7/12)이 기존 추정치와 달라서 Block 1 카드 스타일 보정 커밋 진행. 다음 블록부터는
  App.tsx 공식·인터랙션을 그대로 이식.
- (Eunji-Gil) Block 2: 매물유형 탭(원룸/빌라·주택/오피스텔) 클릭 시 App.tsx PRESETS 그대로
  필드값 전환, 재클릭 시 토글 해제, 초기화 버튼은 전 필드 0. 월관리비·통신비 필드가 원래
  만원 단위로 잘못 계산되고 있던 것도 이번에 원 단위로 수정.
- (Eunji-Gil) Block 3: App.tsx의 정확한 계산 공식(WPM=365/7/12, 공실 일수 직접입력)으로
  교체. 기존엔 월순수익 계산에서 소모품비가 빠지던 버그, 영업이익률이 공실률과 무관하게
  고정이던 문제를 App.tsx 기준으로 같이 고침. 원룸/빌라주택/오피스텔/초기화 4케이스를
  node로 독립 검증해서 스크린샷 수치와 정확히 일치 확인.
- (Eunji-Gil) Block 4: "언제부터 돈을 벌까" 게이지를 App.tsx 방식(안전구간 고정 fill +
  현재 공실일수 위치 손잡이)으로 교체, "공실률 시나리오" 표를 퍼센트 구간에서 공실일수
  [3,6,10,15,20,25] 막대그래프+표로 교체(클릭하면 공실일수 슬라이더 이동).
- (Eunji-Gil) Block 5: "비슷한 매물과 비교하면" 시장비교 카드를 App.tsx MarketPositionCard
  그대로 이식 — 지역 드롭다운(전국/서울/경기) + 목업 분포 히스토그램, 상위 N% 계산.
  원룸 기본값 기준 "상위 42%" node로 검증, 스크린샷과 일치.
- (Eunji-Gil) Block 6: 하단 CTA를 App.tsx 그대로(보라 배경 배너, 정적 카피,
  ChevronRight 버튼)로 교체, 시나리오 표에 overflow-x 스크롤 래퍼 추가, 안 쓰는
  죽은 CSS(.field-grid/.calc-detail* 등) 정리. — 이후 세부 조정: 매물유형 탭 위치를
  top-bar 우측 상단→하단 정렬(App.tsx sm:items-end 반영), 지역 드롭다운 화살표
  여백 보정, 계산기 페이지 font-weight 800→700(h1 제외), 포인트 색상(브랜드/수익/
  손실/골드)을 Figma 값 대신 기존에 정의해뒀던 디자인 토큰으로 되돌림, "한 달
  순수익"→"월 순수익"·"월 순익 시나리오"→"순수익 시나리오" 카피 수정, App.tsx
  Field/Hint 컴포넌트 확인 후 빠져있던 입력 필드별 설명 툴팁(i 아이콘) 11개 복원.
- (Eunji-Gil) 이후 세부 조정 계속: 툴팁 디자인·아이콘 크기/정렬 여러 차례 보정,
  카드 그림자·배경 명도 대비로 입체감 보강, 하단 CTA 색상 조정. 그 과정에서 발견한
  버그: 공실률을 적자 구간으로 옮기면 hero-card가 세로로 늘어나던 문제 — 원인은
  fmtWon()이 음수 부호로 쓰던 유니코드 마이너스(−)가 Pretendard에 없어 Malgun
  Gothic으로 폴백되면서 그 줄만 줄높이가 커진 것. ASCII 하이픈으로 교체해서 해결.

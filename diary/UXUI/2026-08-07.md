# 2026-08-07

## origin/main 머지 (손익분기 게이지 라벨 충돌 해결)

- `main`(#298 손익분기 게이지에 라벨 추가, 라건호)을 `feature/UXUI`로 가져오다가
  `web/portal.py`의 게이지 CSS·HTML·JS 3곳에서 충돌.
- UXUI 쪽엔 이 부분에 독자 변경이 없어 전부 `main`의 라벨 버전으로 채택
  (구 `o_gbe` id는 `o_gbeflag`로 대체됨). `py_compile`로 문법만 확인.

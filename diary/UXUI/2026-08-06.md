# 2026-08-06

## 계산기(/calc) 아이콘·헤더·KPI 정리

- 이모지 아이콘(🧮 ↗ 🗋 ⚠ ⌃ 📉)을 전부 브랜드 컬러(#4D2EE9) 인라인 SVG로 교체.
  OS/폰트별 이모지 렌더링 차이 없애기 위함.
- `공실률 시나리오` 섹션: 제목에 붙어있던 계산식(`연 매출 × (1−공실률) − 연 임차원가`)을
  제목 밖으로 빼고, info 아이콘(hover 데스크톱 / 클릭 모바일) 툴팁으로 이동.
  기존 손익분기 경고 배너(`vac-notice`)는 삭제했지만 아래 공실률 표는 그대로 유지.
- info 아이콘 크기 16px 고정, 제목과의 간격·툴팁 위치(화살표 포함) 여러 차례 조정.
- KPI 3박스 순서를 손익분기 → 영업이익률 → 주 순익으로 변경.
- `월 순수익`(hero-num) font-weight 900→700로 낮춤.
- 섹션 라벨·힌트 문구 축약("① 입력 — 내가 넣는 값" → "① 입력" 등).
- 위 변경은 `/calc` 라우트가 로그인 여부와 무관하게 같은 템플릿을 쓰므로
  로그인 후 화면에도 동일하게 반영됨을 확인.

## 계산기 요약 문구 정렬 + 라벨 다듬기

- `details.more summary`(세부 비용 · 계산 내역 접기 버튼)가 body의 `line-height:1.55`를
  상속해서 아이콘·텍스트가 박스 안에서 위로 치우쳐 보이던 문제 → `line-height:1` 적용해 수정.
- 위 작업 중 사용자가 직접 `details.more`의 `padding-top:14px`를 제거하면서, 자체 padding이
  있는 `calc-detail`(계산 내역 박스)은 괜찮았지만 `.more`만 쓰는 세부 비용 박스가 위 구분선에
  붙어버림 → `details.more:not(.calc-detail)`로 범위를 좁혀 padding-top만 복원.
- `sec-label` 폰트 크기·굵기 조정, 번호(①②) 제거, 안내 문구·요약 텍스트 축약
  ("계산 내역 · 검증용" → "계산 내역" 등) — 사용자가 직접 수정.

## 받는 돈 박스 슬라이더 여백

- "받는 돈" 박스가 "내가 내는 돈" 박스보다 마지막 줄(기대 공실률 슬라이더)이 답답해
  보인다는 피드백 → 원인은 "내가 내는 돈"은 마지막 필드 뒤에 구분선+padding이 있어
  여유로워 보이지만 "받는 돈"은 필드 뒤에 슬라이더가 바로 붙어서였음.
  `.slider-row` margin-top 18px→28px로 늘려 두 박스의 세로 리듬을 맞춤.

## origin/main 머지 (계산기 페이지 충돌 16곳 해결) + CTA 문구 색상 정리

- `main`이 로컬 대비 23커밋 앞서 있었고, 그중 팀원의 `계산기 페이지 라이트 테마
  리디자인(#274)`이 우리와 같은 `web/portal.py` CALC_PAGE 영역을 독립적으로 다시 만든
  상태라 머지 시 16곳 충돌 예상 → 임시 브랜치(`tmp-merge-check`)에서 실충돌 범위만
  먼저 확인 후 삭제, 이후 `feature/UXUI`에서 실제로 머지·해결.
- 계산기 페이지 담당이 이쪽이라 16개 충돌 전부 HEAD(이번 세션 작업분: SVG 아이콘,
  info 툴팁, 로그인 시 cta-row 숨김, `#__nav` margin 수정 등) 유지로 해결.
  단 계산기와 무관한 랜딩 문구 1곳(`{REGION_TEXT} 전 지역` — 부산·천안 지역 확장 반영)은
  main 쪽을 채택.
- 다른 41개 파일(파이프라인·지도·채팅 등)은 전부 자동 머지, 충돌 없음.
- CTA 문구(`이 매물은 한 달 약 N일까지...`)에서 "약 N일"만 강조색 적용 시도
  → 브랜드 퍼플 → 임의의 초록(#2FE98C, 브랜드와 동일 채도/명도) → 최종적으로 기존
  `--profit`(#148A5E, `.pos` 클래스) 재사용으로 정리. 새 색상 변수를 따로 만들지 않고
  이미 있는 "수익=초록" 의미 그대로 재사용.
- CTA 하단 설명 문구의 em dash(—)를 쉼표로 교체.

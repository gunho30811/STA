# 2026-08-02

## 계산기(/calc) 로그인 상태 네비바 margin 통일

- 로그인 시 `auth._inject_nav`가 삽입하는 통합 네비바(`#__nav`)에 margin 규칙이 없어서
  비로그인 헤더(`.hd`, `margin:-20px -14px 20px`)와 상하 간격이 어긋나던 문제 발견 →
  `#__nav`에도 동일한 margin 적용해 통일 (`web/portal.py`).
- Supabase DB를 삭제해서 더 이상 실제 로그인이 안 되는 상태라, 로그인 상태 화면 검증은
  `web/portal.py`·`web/auth.py`를 수정하지 않고 `CALC_PAGE`/`_nav_html()`을 read-only
  스크립트에서 import해 세션을 흉내 내는 방식으로 로컬 프리뷰 렌더링해서 확인.

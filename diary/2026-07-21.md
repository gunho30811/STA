# 2026-07-21

## 삼삼 커버리지·부동산 지하·신규 지역 (#261)

사용자 피드백: ①33엠투엔 있는데 우리 표기가 적다(단대오거리 등) ②지하 매물 아쉽다 ③삭제 매물 정리 ④신규건 지역.

### 진단 (실데이터)
- API 수도권 16,682 vs DB 17,433: **API엔 있는데 DB에 없음 151건(신규 미반영)**,
  **DB엔 있는데 API에 없음 902건(삭제됐는데 지도에 잔존)**.
- `record_churn`이 정의만 되고 **main에서 호출 안 됨** → 삭제 미처리·churn 0행.
- 삼삼 매물 자체는 지하 거의 없음(반지하 0/60). **지하는 부동산(네이버) 얘기** — 네이버 층은
  음수 없음, summary 텍스트로만 판별(반지하 905·지하 2302·지층 1857건). 기존엔 반지하만 제외,
  **지층·지하층은 안 걸러짐**.
- 목록 room 객체에 isNew·isSuperHost 플래그 존재.

### 수정
- **삭제 정리 + 신규 집계** `prune_and_track()`(record_churn 대체, main에서 호출): 현재 목록에
  없는 수도권 매물을 samsam_listings·samsam_cal_last에서 삭제(목록 5000건↑일 때만=차단 시
  대량삭제 방지). 신규(처음 등장) 매물을 동별 집계 → kv_cache 'samsam_new_by_dong'. 시도별 churn도 적재.
  유형 필터 前 전유형 목록으로 처리 → 오피스텔 일일 크롤에서도 전 유형 삭제/신규 반영.
- **부동산 지하 제외 강화**: _basement_exclude() 헬퍼로 반지하·지하층·지층 3패턴을 summary·
  summary_tags 양쪽에서 제외(‘지하’만 쓰면 지하철 오탐 → 안전 표기만). 지도·추천 양쪽 적용.

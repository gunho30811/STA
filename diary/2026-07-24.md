# 2026-07-24

## 네이버 부동산 매물 안 나옴 → 원인규명 + 크롤 경로 재설계

- **증상**: 부동산 매물 뷰어가 비어 보임. 원인 = 서버 네이버 크롤이 IP차단(ERR_CONNECTION_RESET)으로
  실패 → `listings` 미갱신 → 뷰의 "최근 7일" 필터가 0건으로 뷰어를 비움.
- **즉시 복구(#267)**: `nl_live`/`nl_base` 뷰에서 최신일 필터 완전 제거. 부분 크롤에도 안 비고
  전량(약 58만건) 표시. 컨테이너 내부에서 뷰 재생성 완료.
- **서버 네이버 크론 비활성화**: 실패만 반복하므로 `/etc/cron.d/rendit-crawl`을 주석 처리(삼삼은 로컬
  rendit-samsam-daily가 담당).
- **핵심 원인규명**: 예약률 30%+ 동 크롤이 오라클에 안 쌓임(93k 중 15건만). Supabase도 07-15가
  최신 → 어디에도 안 남음. **장시간(2h) SSH 터널 직적재에서 write 유실**이 원인으로 확정.
- **경로 재설계(#269, #270)**: 네이버 로컬 크롤을 터널 직적재 → **Supabase 적재 후 서버 sync**로 변경
  (삼삼과 달리 네이버만). 검증: 역삼·논현 2개 동 테스트 **1611/1611 무손실 적재**, 전체 크롤 라이브로
  07-24 누적 증가 확인.
- **스케줄러**: `deploy/crawl_naver_local.py`(+`.bat`), Windows 작업 `rendit-naver-daily` 매일 02:00
  등록(삼삼 05:00보다 먼저 → 삼삼 build_integrated가 최신 네이버 반영). 대상=예약률 30%+ 동 426개.
- 전체 크롤 완료: **Supabase에 07-24 수도권 249,034건 무손실 적재**(터널 15건 대비 압도적).
  서버 sync 후 **오라클 listings 813,225건 / nl_base(뷰어) 682,186건**으로 신선 데이터 반영 확인.
  (trigger_server_sync는 SSH 읽기 1800s 타임아웃으로 "불확실" 표시됐으나 서버측 sync는 완료. 6시간
  보완 크론 `/etc/cron.d/rendit-sync` 정상.)

## 네이버 대상 동 자동 갱신 (#272)

- "예약률 낮은 곳 제외" 정책이 고정 목록으로 낡지 않게, 삼삼 일일 크롤(snapshot 직후)에
  `pipeline/samsam/gen_naver_dongs.py` 추가 → 최신 samsam_snapshots 예약률로 **수도권 예약률 30%+
  동을 매물수 가중 동평균으로 재계산**해 `deploy/naver_target_dongs.txt` 갱신. 다음 네이버 02:00
  크롤이 사용.
- 산정 방식은 사용자와 확정: 전 유형 종합 30%+(가중 동평균). 목록 426→**252동**(07-23 스냅샷).
- 안전장치: 재계산 결과가 기존의 50% 미만이면 파일 유지(부분 스냅샷 방어).
- 운영 정리: 네이버 02:00 → 삼삼 05:00(로컬), 둘 다 PC 켜져 있어야 함. 오라클 크롤 크론은 비활성,
  sync/backup 크론은 유지.

# 2026-07-03

## 삼삼 예약률 갱신 — 공실 우선 로테이션 + 조기 중단 즉시 종료 (fix/samsam-vacancy-priority-rotation)

전날(7/2) "삼삼엠투 크롤링 자동화(매일)" 스케줄 run이 실패해 원인 추적.

- **실패 원인**: 코드 버그가 아니라 안전장치 정상 동작. 예약률 갱신 초반 200건에서 서버가
  HTTP 200(ok)은 주는데 스케줄 데이터는 0건으로 응답 → 소프트차단으로 판단해 DB 미반영·`exit 1`
  (4b16e04에서 넣은 0 오염 방지 로직). PR #72(현재접속자수)는 Vercel 통과로 무관.
- **로그의 이상점 = 30분 공백**: 22:20 차단 판단 후 종료가 22:50. 원인은 `REFRESH_CHUNK=2000`건을
  ThreadPool에 한 번에 submit → 200건에서 `break`해도 `with ThreadPoolExecutor` 종료가 제출된
  ~1800건이 다 끝날 때까지(2워커+20초 쿨다운) 블로킹. **→ `try/finally`로 바꿔 조기 중단 시
  `pool.shutdown(wait=False, cancel_futures=True)`로 미시작 요청 취소, 30분 대기 제거.**
- **공실 우선 로테이션**(사용자 제안): 이미 한 달치 예약 잡힌 매물은 매일 다시 봐도 안 바뀜.
  `booked_days_1m>0`이면 마지막 확인 후 `BOOKED_COOLDOWN_DAYS`(기본 7, env 조정)일 동안 재갱신
  제외, 공실은 매 실행 갱신 → 하루 5000건 예산을 '새 예약이 잡히는' 공실+쿨다운 경과분에 집중.
  쿨다운 지난 예약 매물은 다시 대상에 들어와 취소/연장도 반영. `collected_at`(=마지막 확인 시각)
  ISO 문자열 비교로 판정.

## 건물 인기 탭 — 오피스텔(건물명) 검색 + 자체 검색 색인 모듈 (feature/building-name-search)

건물 인기 탭에 건물명(오피스텔 명) 검색을 추가하면서, 검색 로직을 별도 `search/` 패키지로 분리.

- **엔진 선택**: Elasticsearch 검토했으나 제외 — 우리 배포가 Vercel 서버리스(상주 서버 없음,
  읽기전용 FS)라 ES는 외부 클러스터(월과금)가 필요하고, 데이터가 적어(수만 건) 가치 대비 과함.
  SQLite FTS5(trigram)도 검토했으나 **trigram 토크나이저가 3자 미만 질의를 못 잡음**("강남"·
  "한화" 같은 2자 한글 검색이 빈 결과) → 실측 확인 후 탈락.
- **결정**: 외부 엔진 없이 **순수 파이썬 n-gram(기본 2-gram) 역색인**을 직접 구현(`search/`).
  2자 한글 검색이 바로 되고, JVM·외부서버·trigram 제약 전부 없이 Vercel에서 그대로 동작.
  - `search/text_index.py` `TextIndex`: normalize(공백제거+소문자) → n-gram posting →
    질의도 n-gram으로 쪼개 교집합으로 후보 좁히고 substring 재확인으로 오탐 제거.
- **연동**(`web/samsam_app.py`): 데이터가 프로세스당 1회 로드(`_LC`)되므로 색인도 최초 사용 때
  1회 구축(`_indexes()`, 건물명·역명). `/api/buildings`의 건물명 검색(신규)·역 검색(기존)을
  모두 이 색인으로 태움. 실측: 색인 결과가 기존 순진 substring과 100% 동일(건물명 5203·역명 466).
- **프론트**(`templates/samsam.html`): 건물 인기 탭에 "건물명 검색" 입력 추가(역 검색 옆).
- 앞으로 텍스트 검색은 전부 `search/`를 거치게 확장 예정.

## 삼삼 크롤러 "차단" 재진단 — IP가 아니라 스케줄 엔드포인트 국한

- 재실행(28630764538)도 동일 실패. 다만 **같은 실행에서 매물 목록 API는 15,789건 정상 수신**,
  바로 뒤 **스케줄 API만 `SCSS_001`(성공)인데 `data.schedules` 빈 배열**. → IP/계정 전면 차단이 아님
  (그럼 목록도 막혔어야). 7/2 오전엔 4998건 갱신 성공, 밤부터 빈값 = 스케줄 엔드포인트 선택적
  스로틀 or API 변경 의심. 사용자 브라우저에선 스케줄 데이터 보임 → 봇 요청만 빈값.

## 건물 인기 CSV — 대표 네이버 매물 연락처(전화/중개사/월세/보증금/층수) 추가 (feature/building-csv-contact)

"전화해서 물어보게" 건물인기 CSV에 부동산 연락처를 넣음. 데이터는 이미 DB `naver_listings`에
`agent_phone/agent_office/floor_current/rent_monthly/deposit`로 적재돼 있어 재크롤 불필요.

- `build_integrated.py`: 네이버 SELECT에 `agent_phone,agent_office` 추가. 이미 있던 대표(중앙값
  근접) 매물 `rep` 기준으로 부동산번호·중개사무소·대표월세·대표보증금·대표층수 5컬럼 CSV 출력.
  전화가 JSON 배열 문자열이라 `_fmt_phone`으로 ' / ' 결합.
- `samsam_app.py`: `_load_matches`가 새 컬럼 파싱(구 CSV엔 없으므로 `.get`으로 방어). `/api/buildings`가
  건물별 대표 매물(sample) 연락처를 응답에 포함(사용자 선택: 건물당 대표 1건).
- `templates/samsam.html`: 건물인기 CSV에 5개 컬럼 추가.
- 실제 값은 다음 파이프라인 CSV 재생성 때 채워짐(코드는 구 CSV에서도 안 깨지게).

## 삼삼 스케줄 "빈값" 원인 확정 — 봇 계정 일일 한도 (fix/samsam-schedule-throttle)

실제 매물(102951)로 브라우저 vs 봇을 대조해 원인 확정.

- 미인증 `GET /v1/use-auth/rooms/{id}/schedules` → **401**(AUTH_002). 상세(`/rooms/{id}`)는 미인증도 200.
  즉 스케줄은 인증 필수.
- 사용자 로그인 브라우저: 같은 URL/파라미터로 `data.schedules` 정상(booking 다수). 봇: `SCSS_001`인데
  빈 배열 + `실패 0` → **봇은 인증엔 성공**(아니면 401)하는데 데이터만 빔.
- 사용자 요청 헤더(F12) 확인: `authorization` 헤더 **없음**(알파벳 순 accept-language→cache-control
  사이 공백). → **쿠키 인증**이고 Bearer 토큰 방식 아님. (Bearer 추가안은 폐기.)
- 타임라인: 7/2 오전 4,998건 갱신 성공 → 저녁부터 첫 요청부터 빈값 → **계정당 스케줄 조회 일일
  소프트한도**(초과 시 401 아닌 200+빈배열) 정황. 목록·상세는 같은 쿠키로 멀쩡 = 스케줄 엔드포인트 한정.

대응(저위험 2종 병행):
- `crawler.py`: 브라우저가 늘 보내는 `abid`(익명 UUID, 실행당 1개)·`client-language: ko` 헤더 추가 —
  봇 식별로 걸러지는 경우 대비(쿠키 인증엔 무영향).
- 워크플로: `SAMSAM_REFRESH_DAILY_LIMIT` 5000→3500 하향(한도 아래 여유). 공실 우선 쿨다운(#89)으로
  실제 대상도 줄어 상한엔 잘 안 닿음. 계정 한도는 하루면 리셋 예상 — 다음 실행에서 확인.

## 삼삼 스케줄 다중 계정 순환 — 하루 커버리지 배증 (feature/samsam-multi-account)

스케줄 한도가 계정당이라는 진단(#92)에 이어, 계정을 여러 개 쓰면 한도를 우회하는지 시도.

- `crawler.py`: `_get_accounts()`가 `SAMSAM_EMAIL/PASSWORD` + `EMAIL2/PASSWORD2`(…9)를 모음.
  예약률 갱신 대상을 계정 수만큼 연속 슬라이스로 나눠(각 슬라이스 ≤ `REFRESH_DAILY_LIMIT`),
  계정 k가 슬라이스 k를 담당. 슬라이스가 계정 한도보다 작아 도중 소진이 잘 안 나고, 소진돼도
  그 계정 몫만 스킵하고 다음 계정으로(전체 실패 아님). 전 계정 데이터 0일 때만 실패 종료.
  하루 상한 = `REFRESH_DAILY_LIMIT × 계정 수`(3500×2=7000).
- `crawl-samsam.yml`: `SAMSAM_EMAIL2/PASSWORD2` 시크릿 배선(없으면 빈값 → 1계정).
- 부수효과: 이 실험이 **한도가 계정당인지 IP당인지도 판별** — 2번 계정도 같은 GHA IP인데
  데이터가 오면 계정당, 여전히 빈값이면 IP당(그땐 샤딩/멀티IP 필요).
- 제어흐름 시뮬 검증: 2계정 전부 데이터→7000, 계정1 소진·계정2 정상→3500(성공), 전 계정 소진→실패.

## 통합 CSV 재생성 — 부동산 연락처 실제 채움 + 계정 조회 확인 (chore/regen-integrated-csv-contacts)

- 건물인기 CSV의 부동산번호·중개사무소·월세·보증금·층수가 전부 빈값이란 제보 → 원인은
  `data/net_profit_integrated.csv`가 구 형식(새 컬럼 없음)이었음(#91 코드만 머지되고 CSV 재생성 전).
  DB(pg8000 로컬 접속)에 `agent_phone` 21,678/21,688건 있음 확인 → `build_integrated.py` 재실행으로
  CSV 재생성(매칭 3,192건 중 3,185건 전화 채워짐). 커밋해 배포 반영.
- 덤: 로컬에서 계정 1·2 각각 로그인→스케줄 조회 테스트 → 둘 다 정상(102951=5일 등 3/6 데이터,
  0일은 진짜 공실). 오전 소진됐던 계정 한도가 리셋됨 확인. abid 헤더 요청도 정상.

## 웹에 1달/2달/3달 예약률 표시 (feature/samsam-occ-1-2-3month)

크롤러는 그대로 두고(이미 DB에 booked_days_1m/2m/3m 있음), 웹에 호라이즌별 예약률을 노출.

- 배경: 주 단위 단기임대라 먼 달은 대부분 '아직 안 잡힌' 공백이지만(64%가 30일 이후 예약 0),
  일부(20%)는 31~90일에 16일+ 예약 = '몇 달치 미리 찬 핫한 매물' → 멀티 호라이즌이 이 세그먼트
  식별에 가치. 데이터는 이미 있으니 표시만 추가(조회범위/블록 결정과 분리).
- `export_jsonl.py`·`samsam_app.SAM_COLS`: export/DB 조회에 booked_days_2m/3m 추가 → lab 재생성.
- `_enrich`: occ2=booked_days_2m/61, occ3=booked_days_3m/91(snapshot.avg_occ_3m 관례, blocked 미보정).
- `/api/buildings`: 건물별 occ2_avg·occ3_avg 추가. 매물목록 API: occ2·occ3 추가.
- `templates`: 건물인기 탭에 1달/2달/3달 예약률 열(+CSV), 매물목록에 2달·3달 열.
- 검증: 수도권 평균 1달 41.6%→2달 28.7%→3달 22.1%(예상대로 감소), 3달 100% 만실 핫매물 정상 노출.

## 건물 예약률 랭킹 탭 (feature/samsam-ranking-leaderboard)

'건물 인기' 탭은 상단 필터 안에서의 순위라, 전 지역 리더보드를 별도 '🏆 랭킹' 탭으로 추가.
- 프론트만: `/api/buildings`(#95에서 occ_avg/occ2_avg/occ3_avg 반환)를 자체 컨트롤(시도·최소매물수)로
  호출해 전역 집계 → 1/2/3달 예약률 중 선택 정렬 → top N에 순위(🥇🥈🥉) 표시. 상단 공유 필터는 무시.
- 3달로 정렬하면 '몇 달치 미리 찬 검증된 핫 건물'이 상위에.

## 회원가입 버그 수정 — 미인증 잔재가 재가입 막음 (fix/signup-unverified-email)

제보: "처음 가입인데 '이미 가입된 메일'이라며 막히고, 인증메일은 옴" (10회+).
- 원인(`auth.py` signup): 이메일 존재 체크가 `email_verified`를 안 봄. 가입 첫 시도가 미인증
  레코드를 INSERT+메일발송하는데, 더블클릭·뒤로가기·재제출 시 그 미인증 잔재 때문에 "이미 가입"
  으로 막힘. 이메일은 이미 나간 뒤라 정확히 제보 증상.
- 수정: **인증 완료(email_verified=TRUE) 계정만 차단**, 미인증 레코드는 재시도로 간주해 입력값·
  인증코드 갱신 후 재발송(UPDATE)하고 verify로 이동. 하루 가입한도는 신규 INSERT에만 적용.
  프론트: 폼 onsubmit에 버튼 비활성화(더블서브밋 레이스 원천 차단).
- 검증: 실제 DB(dict 래퍼)로 미인증=허용/인증완료=차단 확인(트랜잭션 롤백, 무변경).
- 부수효과: 이미 막혀 있던 미인증 잔재 이메일들도 이 수정으로 재가입 가능해짐.

# 2026-06-24

- **Vercel 배포 설정** (`feature/vercel-deploy`):
  - `api/index.py`: `APP_NAME` 환경변수로 naver/profit 앱 분기 (단일 진입점)
  - `vercel.json`: 모든 요청 → `/api/index`, playwright/requests 제외 최소 의존성만 설치
  - Vercel 프로젝트 2개 생성 후 각각 `APP_NAME=naver`, `APP_NAME=profit` 설정하면 됨
  - `DATABASE_URL` secret은 sta-naver 프로젝트에만 필요 (sta-profit은 CSV 직접 읽음)

# 2026-07-12

## 프론트엔드 React 이전 시작 (feature/react-profit-pilot)

- 배경: 프론트가 `templates/*.html`에 HTML+CSS+JS 몰빵 구조 → 뷰어 3개 코드 중복,
  `innerHTML` XSS, 핫리로드 없음(코드 고칠 때마다 서버 재시작), 필터 상태 URL 미반영.
  "데이터가 느리다"는 별개 문제로 확인됨 → **부동산매물 뷰어가 70MB `lab/naver_listings.jsonl`을
  통째로 로드**(첫 응답 ~1.4s)가 원인. React로는 안 고쳐짐(백엔드 이슈). profit은 CSV라 3ms로 빠름.
- 결정: **Vite + React SPA (Flask 백엔드 유지)** + **뷰어 1개씩 점진 이전**. 백엔드/ API는 그대로 두고
  뷰(템플릿)만 교체.
- profit(수익성) 뷰어 파일럿 완료:
  - `frontend/` Vite 프로젝트 신설 (base `/profit/`, dev 프록시로 `/profit/api`·`/auth` → :8000 → dev에서도 세션쿠키 동작)
  - `src/components/`: ProfitList(매물 수익성·시/도 컬럼 포함) / RankTab(순위) / RecoTab(추천) / Detail / Pager
  - 기존 3탭·필터·정렬·상세·페이징 기능 그대로 이식, 디자인 CSS도 이식. `innerHTML` 제거로 XSS 해소.
  - `web/profit_app.py`: index 라우트를 `frontend/dist` 서빙으로 교체(+`/assets/<file>` 라우트). API는 무변경.
  - 빌드/서빙 검증 OK (test_client: index 200·root div·에셋 js 200).
- TODO: gangnam·samsam(+채팅) 이전, Vercel 빌드 연동(현재 vercel.json은 pip만 — dist 빌드 단계 필요).

## 멀티 뷰어 빌드 구조 + gangnam(부동산매물) 이전

- 뷰어가 각각 다른 base(/profit/, /gangnam/)로 마운트돼 base가 빌드마다 달라야 함 →
  **단일 Vite 프로젝트, 뷰어별 멀티 빌드**로 구성:
  - `src/main.jsx`가 `VITE_VIEWER` 상수로 뷰어 App을 정적 분기 import(상수폴딩 → 빌드에 해당 뷰어만 번들)
  - `vite.config.js`: `VIEWER` 환경변수로 base=`/${VIEWER}/`, outDir=`dist/${VIEWER}`
  - `src/shared/`(api.js 공용·뷰어 무관 로그인 리다이렉트), `src/profit/`, `src/gangnam/`로 재편
  - package.json: `build`(profit+gangnam), `build:gangnam`, `dev:gangnam` (cross-env)
- gangnam React 이전 완료: 타입/방수 칩, 지역 트리 다중선택(➕칩), 역 반경검색(➕칩), 카드 그리드,
  상세 모달(DB 상세 병합), 근처 삼삼 박스(SamArea) — 기존 기능 그대로. `innerHTML` 제거로 XSS 해소.
  `web/gangnam_app.py`: index를 `frontend/dist/gangnam` 서빙으로 교체(+/assets). API 무변경.
  `web/profit_app.py` DIST는 `dist/profit`로 조정.
- 검증: profit·gangnam 둘 다 빌드→서빙→api/facets OK. 포털 /profit/·/gangnam/ 302(로그인 게이트) 정상.
- 남음: samsam(+통합채팅) 이전, Vercel 빌드 연동.

## 공통 네비(탭) 복구 + samsam(삼삼분석) 이전

- 버그: React 뷰어에서 상단 공통 네비게이션(홈/수익성/삼삼분석/부동산매물/채팅)이 사라짐 →
  원인은 `send_from_directory`(direct_passthrough)라 `auth.py` after_request의 네비 주입이 안 먹은 것.
  → 세 index 라우트를 **index.html을 문자열로 읽어 반환**하게 바꿔 네비 주입 복구.
- samsam React 이전 완료(메인 뷰어 4탭): 건물인기(BuildingView, CSV 다운로드 포함)/랭킹(RankingView)/
  지역트렌드(TrendView, 동적 날짜 컬럼)/옵션영향(OptionView, 옵션표+없는집·있는집 목록). 공용 필터패널
  (시도 복수칩·시군구·동 종속·건물유형·평수·주당). `innerHTML` 제거로 XSS 해소. 헤더정렬·툴팁·판정색 유지.
  `samsam_app.py`: index를 dist/samsam 서빙으로 교체(+/assets). API·`/chat/` 무변경.
- 통합채팅(/samsam/chat/)은 아직 기존 템플릿 그대로(실시간이라 별도 증분으로 남김).
- 검증: samsam 빌드→서빙→api(facets/buildings/analyze) OK, 네비 주입 OK. 전 라우트 302 정상.
- 남음: 통합채팅 이전, Vercel 빌드 연동.

## 통합채팅(/samsam/chat/) React 이전 — 4개 뷰어 이전 완료

- chat을 별도 뷰어로 빌드(base `/samsam/chat/`). vite.config에 BASES 매핑 추가, main.jsx는 if 분기
  (상수폴딩 → 빌드별 해당 뷰어만 번들, dist 깔끔). package.json build:chat 추가.
- `src/chat/`: App(계정+채팅함) / AccountsPanel(계정 연결·해제) / Inbox(방목록·스레드·전송, 20초 폴링) /
  helpers. 공용 api.js에 `sendJSON`(POST/DELETE) 추가. 메시지 렌더는 React 이스케이프라 XSS 자동 해소.
- 코드 가독성 피드백 반영: 채팅 컴포넌트는 컴포넌트별 파일 분리 + 여러 줄 포맷으로 작성(리뷰·수정 쉽게).
- `samsam_app.py`: `/chat/`을 dist/chat 서빙으로 교체(+/chat/assets). 채팅 API(계정/방/메시지/전송)는 무변경.
- 검증: chat 빌드→서빙→api/accounts OK, 네비 주입 OK. 각 dist App.css/js 1개씩(린 빌드). 전 라우트 302.
- **뷰어 4개(profit·gangnam·samsam·chat) React 이전 완료.** 남음: Vercel 빌드 연동, 기존 뷰어 가독성 정리(선택).

## 레거시 정리

- React 이전으로 죽은 코드 제거:
  - `templates/*.html` 5개 전부 삭제(profit/gangnam/samsam/samsam_chat/index — 이제 참조 0)
  - `web/app.py`(옛 네이버 뷰어 :5000, 포털에 마운트 안 됨, gangnam이 대체) 삭제
  - profit/gangnam/samsam_app에서 죽은 `render_template` import + `template_folder=` 제거
  - `lab/run.log` 삭제 + `.gitignore`에 `*.log`·`frontend/node_modules`·`frontend/dist` 추가
- README를 새 구조로 갱신(통합 포털 :8000 + React 뷰어 4개, frontend 빌드/개발 안내, 폴더구조).
- 검증: 포털 부팅 에러 없음, 4개 뷰어 302(로그인 게이트) 정상.
- 남음: gangnam 70MB jsonl → DB 쿼리 전환(성능), Vercel 빌드 연동(+SECRET_KEY).

## gangnam 데이터 계층: 70MB 파일 → DB(naver_listings) 쿼리 전환

- 병목이던 `lab/naver_listings.jsonl`(70MB, 115,645건) 통짜 로드 제거. facets/stats/listings를
  DB SQL로 전환(WHERE/LIMIT/ORDER BY로 필요한 페이지만).
- 필터 SQL화: 타입/지역트리(시·구 분리는 SQL `split_part`/`right`)/방수/보증금·월세·평수 범위/키워드(ILIKE)/업무용.
  역반경은 bbox로 SQL 선필터 후 파이썬 haversine 정밀 판정. 월순수익(net) 정렬·필터는 삼삼 계산이 필요해 파이썬.
  `%s`+`LIKE '%구'` 충돌 회피 위해 `right(sigungu,1)='구'` 사용.
- 근처삼삼(15MB samsam 인덱스)·상세모달(DB)·CSV는 그대로.
- **파리티 검증 통과**: 전체·타입·시도·동·방수·지역 9케이스 total 파일버전과 정확히 일치.
  성능: 기본 목록 첫 요청 159ms(콜드스타트 70MB 로드/1.4초 사라짐), facets 563ms, stats 299ms.
  (월순수익 정렬만 전 매물 계산이라 ~4초 — 원래도 그랬던 드문 경로.)
- 참고: 이제 gangnam은 `lab/naver_listings.jsonl`을 안 읽음 → 그 70MB 파일은 git에서 제거 가능(별도 결정).

## 죽은 코드/데이터 제거 (DB 전환 후속)

- `lab/naver_listings.jsonl`(70MB) 삭제 — gangnam이 DB 조회로 바뀌어 읽는 코드 0(주석만 남았음).
  export_jsonl.py가 재생성해도 다시 안 딸려오게 `.gitignore`에 추가.
- gangnam_app 죽은 함수 제거: `_is_office`·`_split_sgg`(정의만 남고 호출 0 — 각각 SQL로 대체됨).
  모듈 독스트링·주석도 DB 기준으로 갱신.
- `pipeline/enrich_address.py`는 살아있는 스크립트(삼삼 주소 크롤→data/samsam_address.jsonl)라 보존.
- 검증: facets/listings/stats 200, 죽은참조 없음.

## 배포 준비 (Vercel) + main 머지

- Vercel은 Git연동 자동배포(main push → api/index.py Flask 서버리스). vercel.json은 pip만 설치하고
  React를 안 빌드 → dist가 없으면 뷰어가 "빌드 없음"으로 깨짐.
- 해결: **`frontend/dist`를 커밋**(gitignore에서 해제). Vercel Python 함수가 lab/data 파일처럼 이 정적
  산출물을 그대로 서빙하므로 별도 빌드스텝 없이 확실히 동작. 총 ~865KB(4뷰어 × index/App js·css).
  `.gitattributes`로 `frontend/dist/** -text`(EOL 변환 방지, 해시-내용 일치). 옛 최상위 dist 잔재 정리.
- 프론트 수정 시: `cd frontend && npm run build` 후 dist 변경분도 함께 커밋해야 배포에 반영됨.
- ⚠️ 배포 전 Vercel 환경변수 `SECRET_KEY` 설정 필요(현재 기본값 dev-insecure면 세션쿠키 위조 위험).

## 초기 렌더 개선: 헤더(뼈대) 즉시 표시

- 문제(프로덕션): auth.py가 심는 `#__nav`는 즉시 뜨는데 `#root`는 너무 늦게 뜸.
  원인은 profit/gangnam/samsam App이 `facets` API 도착까지 화면 전체를 "불러오는 중"으로 막음
  (프로덕션은 서울 DB 왕복이라 느림) → nav만 먼저 뜨고 한참 뒤 콘텐츠.
- 수정: 각 App이 마운트 즉시 **헤더·탭 뼈대를 렌더**하고, 데이터 의존 영역만 안쪽에서 로딩 표시.
  → nav와 root(뼈대)가 거의 동시에 뜨고, 느린 데이터만 나중에 채워짐.
- 남은 개선(별개): 클라이언트 응답 캐시 + Cache-Control(재방문/재클릭 즉시). facets+stats 합치기.

## facets 다이어트 — 지역 트리 lazy + 업무용수 async (성능 1/2)

- 측정: gangnam `/api/facets`가 113KB·734ms로 제일 뚱뚱(동993+지역1067+역589 전부 미리 보냄 +
  업무용수 세느라 summary ILIKE 전체 스캔). 반면 listings는 크기 무관(147KB가 25KB보다 빨랐음)
  → 병목은 "크기"보다 "요청당 DB비용/왕복". 단 facets만은 진짜 큰 요청이라 쪼갬.
- 변경(gangnam_app): facets는 이제 {sido, types, stations}만(가벼움). 지역 트리는
  `/api/regions?sido=` 로 시/도 고를 때만 lazy 로드(캐시), 업무용수는 `/api/office_count` async.
- 프론트(gangnam/App): regionTree 캐시 상태 + officeCount 비동기. 시/도 선택 시 그 시/도 트리만 fetch.
- 효과(워밍): facets 734ms/113KB → 163ms/15KB. regions(서울) 130ms/18.5KB는 고를 때만.

## 클라이언트 응답 캐시 (성능 2/2)

- shared/api.js `getJSON`에 세션 메모리 캐시 추가(경로별, 기본 TTL 60초). 같은 요청 재클릭·
  재방문 시 네트워크 0으로 즉시. `clearCache()` + POST/DELETE(sendJSON) 후 자동 무효화.
- 분석 뷰어(profit/gangnam/samsam)의 facets·listings·regions 등은 기본 캐시 적용(재클릭 즉시).
- **채팅은 20초 폴링이라 항상 신선해야 해서 `{ttl:0}`로 캐시 제외**(App accounts/rooms, Inbox messages).
- 측정: listings 크기가 시간을 좌우하지 않으므로(왕복 비용이 지배) 재요청 제거가 체감에 가장 큼.
- (후속 여지) 서버 Cache-Control 헤더, facets+stats 한 요청 합치기.

# 2026-06-20

- 퍼블릭 전환 전 보안 점검: `data/config.json`에 삼삼엠투 계정 비밀번호가 평문으로 들어있던 것 발견.
  `classify_btype.py`를 `SAMSAM_EMAIL`/`SAMSAM_PASSWORD` 환경변수(`.env`, 없으면 실행 시 직접 입력)로
  바꾸고, `config.example.json` → `.env.example`로 교체. `.gitignore`에 `.env`, `data/config.json`,
  `__pycache__/` 추가.
- 초기 커밋 후 GitHub 퍼블릭 레포(`gunho30811/STA`) 생성, `main` 브랜치로 push 완료
  (`data/` 내 DB·jsonl 원본도 포함해서 커밋하기로 결정).
- 폴더 구조 정리: `web/`(Flask 앱 `app.py`, `profit_app.py`), `pipeline/`(크롤·가공 스크립트 6개)로 분리,
  `db.py`는 양쪽이 공유하므로 루트에 유지. 이동에 따라 경로 계산(BASE/DATA)과 `import db`/`import crawler`
  경로를 모두 수정하고 두 Flask 앱 재기동해서 정상 동작 확인.
- 인프라 아키텍처 결정 (구현은 아직 안 함, 결정만):
  - **DB**: Supabase(Postgres) 채택. 처음엔 Turso(SQLite 호환, 코드 변경 거의 없음)를 검토했으나,
    추후 로그인/결제 기능까지 갈 계획이라 Supabase가 Auth+Postgres+Stripe 연동까지 한 플랫폼에서
    되므로 이후 재마이그레이션 비용을 피하기 위해 선택. `db.py`는 Postgres 문법으로 다시 써야 함.
  - **앱 호스팅**: Railway. GitHub 연결 시 push마다 자동 배포, 콜드스타트 슬립이 없어 결제 기능 들어간
    서비스에 적합 (Render 무료 티어는 15분 비활성 슬립이라 부적합 판단).
  - **주간 크롤 스케줄**: GitHub Actions 스케줄 워크플로(cron). 서버 없이 GitHub의 클라우드 러너가
    매주 `pipeline/` 스크립트를 실행하고 Supabase에 직접 write, 실행 후 러너는 사라짐. 무료.
  - **데이터 흐름**: GitHub Actions(주 1회, 일시적) → Supabase Postgres(상시, 진짜 저장소) →
    Railway의 Flask 앱이 Supabase를 읽어 서빙. 로컬 `data/*.db`·git 커밋은 테스트용으로만 남기고
    추후 git 추적에서 제거 예정.
- 2인 협업 워크플로 정리: `main` 보호 + `feature/`·`fix/` 브랜치 + PR 방식으로 진행 (`CLAUDE.md` 참고).
- 개발일지를 `DEVLOG.md` 단일 파일 대신 `diary/YYYY-MM-DD.md` 날짜별 파일 구조로 변경.
- gh CLI 인증 완료 (`gunho30811` 계정), 위 인프라 결정 사항을 GitHub Issue 5개로 전환해서 등록:
  [#1](https://github.com/gunho30811/STA/issues/1) DB 마이그레이션(Supabase),
  [#2](https://github.com/gunho30811/STA/issues/2) Railway 배포,
  [#3](https://github.com/gunho30811/STA/issues/3) GitHub Actions 주간 크롤,
  [#4](https://github.com/gunho30811/STA/issues/4) Collaborator 초대 + main 보호,
  [#5](https://github.com/gunho30811/STA/issues/5) 로컬 DB 정리(#1 의존).
  다른 작업자가 합류하면 #4부터 처리하고 나머지 Issue를 assign해서 분배하기로 함.
- `main` 브랜치 보호 규칙이 실제로 적용된 것을 확인 (직접 push 막힘 → 이후 전부 브랜치+PR로 진행).
- 매물 수익성 테이블 개선 (4개 PR로 분리, 전부 머지 대기 중):
  - [#7](https://github.com/gunho30811/STA/pull/7) `가격배수`(삼삼월÷네이버월총) → `네이버÷삼삼주당`(네이버월총÷삼삼주당)으로 교체.
    매물 수익성/동별 베스트/구별 공실률/역별 공실률 테이블 전체에 엑셀식 헤더 클릭 정렬(오름차순/내림차순 토글) 추가.
  - [#8](https://github.com/gunho30811/STA/pull/8) `data/vacancy_by_gu.csv`를 (시도,시군구)로 조인해서 구공실률%/구경쟁매물수 컬럼 추가
    (855/856 매칭). 정렬 검증 로직에 신규 필드가 빠져 profit으로 fallback되던 버그 같이 수정.
  - [#9](https://github.com/gunho30811/STA/pull/9) 구공실률 필터(`vacancy_max`) 추가.
  - [#10](https://github.com/gunho30811/STA/pull/10) 동삼삼매물수(같은 동 삼삼 매물수, 자기 제외, 직접 계산·100% 커버) +
    `data/by_station.csv` 조인 역공실률%/역경쟁매물수(매물수≥3 기준) 컬럼·필터 추가.
  - [#11](https://github.com/gunho30811/STA/pull/11) 막힘일을 "운영 안 한 기간"으로 보고 점유율 계산에서 제외
    (점유율=예약일/(전체기간−막힘일)을 30일 전체에 적용해서 1달실현수익 추정). ⚠️ 예약가능일이 매우 적은
    매물은 적은 표본으로 점유율이 과대추정될 수 있음(예: bk=3,막힘일=27 → 점유율 100%) — 사용자 확인 후
    일단 이대로 적용, 표본 크기 보정은 다음 단계로 보류.
- 삼삼(33m2) 매물 상세페이지가 Next.js 서버렌더링이라 로그인/Playwright 없이 일반 HTTP GET + 정규식만으로
  도로명/지번 주소(층 정보 포함)를 가져올 수 있는 것을 확인. `pipeline/enrich_address.py` 신규 작성해서
  officetel_raw.jsonl+oneroom_raw.jsonl의 전체 매물(16,838건, 중복 제거 후) 대상으로 `data/samsam_address.jsonl`에
  이어받기 지원하며 백그라운드로 수집 중 (0.5초 간격, 약 3~4시간 예상). 완료되면 `build_integrated.py`의
  `strict_match()`에 층 정보를 반영해 같은 건물 다른 층끼리 잘못 매칭되는 문제를 개선할 계획.

# 2026-06-22

- **PR #12** (`feature/db-supabase-migration`): main에 #9~#11 머지 이후 충돌 발생.
  `build_integrated.py`에서 `dong_count`(동 경쟁매물수)·`avail_days`/`occ_rate`(막힘일 보정 점유율) 추가된
  변경사항을 Supabase 단일 쿼리 버전과 수동으로 합친 뒤 rebase → force push 완료.
  Postgres 소문자 컬럼 접근 (`articleName` → `articlename`, `articleNo` → `articleno`) 3곳 추가 수정.
- **이미지 미표시 버그 수정**: `templates/index.html`에서 Postgres가 컬럼명을 소문자로 반환하는 특성으로
  `x.imgUrl` 등 camelCase 키 10개가 JS에서 undefined로 처리되던 문제 수정.
- Railway 배포(#2) 검토 → 유료($5/월~)임을 확인 후 보류.
- **삼삼×네이버 매칭 품질 개선** (`pipeline/build_integrated.py`):
  - 면적 필터: ±3m² 절대값 → ±15% 상대값으로 변경 (`AREA_ABS` → `AREA_PCT=0.15`)
    결과: 오피스텔 기준 716개 → 1,053개 매칭 (47% 증가, 10평±5m² 수준이 이제 통과)
  - 층수 필터 추가: 삼삼 name에서 `N층` 파싱, 네이버 `floorInfo` 파싱 → 둘 다 있을 때만 적용 (약 1.4% 해당)
  - 건물 단위 통계 추가: 면적 필터 전 같은 건물 후보 전체 기준 `건물매물수`, `건물월세최저/중간/최고` CSV에 추가
  - rent 상한 필터: `rent BETWEEN 5 AND 2000` (오류 데이터 100억/월짜리 제거)
- **매칭 로직 2차 수정** (오매칭 49% → 0.2%):
  - 원인: 기존 로직이 좌표 50m OR 건물명 - 둘 중 하나만 맞아도 후보로 넣음 → 인접 다른 건물이 잡힘
  - 수정: 삼삼에 건물명 있으면 건물명 매칭만(500m sanity), 없으면 GPS 15m 이내만 허용
  - 결과: 1,128개 → 569개 (건물명 일치 44% → 89%)
- **네이버 API 지번 조사**: 목록/상세 API 모두 지번 미제공 (`exposureAddress`는 동 수준까지만). 지번 기반 매칭 불가 확인.
- `web/profit_app.py`: 새 컬럼 `PROFIT_MAP`에 추가, `templates/profit.html` 테이블에 건물 통계 4컬럼 추가, 5001 서버 재시작.
- **#5 data/*.db, *.jsonl git 추적 제거**: Supabase 이전 완료 후 로컬 DB 5개 `git rm --cached`,
  `.gitignore`에 `data/*.db`, `data/*.jsonl` 추가, README 폴더 구조 및 db.py 설명 업데이트.
- **#2 Render 배포 설정**: Railway 유료 → Render 무료 플랜으로 변경.
  `render.yaml` 추가 (sta-naver: Supabase+DATABASE_URL, sta-profit: CSV 직접 읽기).
  gunicorn으로 서빙, `$PORT` 환경변수로 포트 자동 할당. `requirements.txt`에 gunicorn 추가.

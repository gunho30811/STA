__pycache__/
*.pyc
.env
data/config.json
.claude/
data/*.db
data/*.jsonl

# gangnam이 DB 조회로 전환돼 더는 안 씀(export_jsonl.py가 재생성해도 커밋 안 함)
lab/naver_listings.jsonl

# 런타임 로그 · 프론트 의존성
*.log
frontend/node_modules/
# frontend/dist 는 커밋한다 — Vercel(Python 서버리스)이 이 정적 산출물을 그대로 서빙하므로.
# (프론트 수정 시 `npm run build` 후 dist 변경분도 함께 커밋)

# PyInstaller 빌드 산출물
dist_exe/
build_exe/
*.spec
_selftest_naver.db
selftest_result.txt

# 개인용 분석 리포트·스크린샷(로컬 전용, 저장소 미포함)
rendit_*.pdf
image.png
# 크롤러 exe/스크립트로 뽑은 개인 작업용 내보내기(침구제공 리스트 등) — 레포에 안 올림
samsam_침구제공*
samsam_매물_*.csv
naver_매물_*.csv

{
  "name": "sta-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "삼삼 x 부동산 뷰어 React 프론트엔드 (Vite). 뷰어별 멀티 빌드. Flask JSON API 소비.",
  "scripts": {
    "dev": "cross-env VIEWER=profit vite",
    "dev:profit": "cross-env VIEWER=profit vite",
    "dev:gangnam": "cross-env VIEWER=gangnam vite",
    "dev:samsam": "cross-env VIEWER=samsam vite",
    "dev:chat": "cross-env VIEWER=chat vite",
    "build:profit": "cross-env VIEWER=profit vite build",
    "build:gangnam": "cross-env VIEWER=gangnam vite build",
    "build:samsam": "cross-env VIEWER=samsam vite build",
    "build:chat": "cross-env VIEWER=chat vite build",
    "build": "npm run build:profit && npm run build:gangnam && npm run build:samsam && npm run build:chat",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "cross-env": "^7.0.3",
    "vite": "^5.4.2"
  }
}
